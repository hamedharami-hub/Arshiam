# Firebase application settings are kept in firebase-applet-config.json.
# AI features require each user to add a Gemini API key in the app settings.
# Server only: Firebase service account JSON for per-user assistant grants.
# Configure this in the Vercel environment. Never prefix it with VITE_ or commit its value.
FIREBASE_SERVICE_ACCOUNT_JSON=
