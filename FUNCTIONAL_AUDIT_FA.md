# گزارش بررسی عملکردی ARSHNAZ

## جمع‌بندی

ساختار برنامه از نظر جداسازی مسیرها، سرویس داده، cache، عملیات تسک و اجزای ویرایش نوت منظم است. تست‌های موجود در زمان بررسی با موفقیت اجرا شدند و build وب نیز بدون خطا انجام شد. با این حال، چند ریسک عملکردی در همگام‌سازی نوت‌ها، ذخیرهٔ cache، به‌روزرسانی خوش‌بینانهٔ تسک‌ها و کار با ویرایشگر در موبایل شناسایی و در همین مرحله اصلاح شد.

## جریان‌های بررسی‌شده

| بخش | مسیر داده و رابط بررسی‌شده | نتیجه |
|---|---|---|
| تسک‌ها | `useTasksData`، `TasksView`، `taskService`، درخت والد/فرزند و نمایش فولدر | منطق scopeهای امروز، فردا، ۷ روز آینده، inbox و فولدر وجود دارد. در خطای update، rollback اضافه شد. |
| فولدرها و Kanban | `FolderKanban`، بارگذاری والد/زیرتسک، ستون‌های وضعیت و drag-and-drop | وضعیت محلی پس از خطای شبکه قبلاً باقی می‌ماند؛ rollback اضافه شد. |
| نوت‌ها | `NotesView`، cache آفلاین، Firestore subscription و fallback سرویس دیگر | snapshot خالی قبلاً نادیده گرفته می‌شد و دادهٔ قدیمی می‌توانست باقی بماند؛ merge با صف آفلاین و پاک‌سازی صحیح اضافه شد. |
| نوشتن نوت | `NoteEditorTabs` و `RichEditor` با Markdown، rich text، فایل و voice input | نوار ابزار در موبایل عرض زیادی اشغال می‌کرد؛ به نوار افقی قابل پیمایش تبدیل شد. |
| نوت جدید | `NewNoteView` و انتخاب فولدر | بارگذاری فولدرها به کاربر واردشده محدود شد. |
| تقویم | `CalendarView` و نماهای ماه، هفته، روز و agenda | query بازه‌ای بر اساس نما انجام می‌شود و اتصال کلی به تسک‌ها درست به نظر می‌رسد. |

## اصلاحات انجام‌شده

### همگام‌سازی نوت‌ها

در subscription نوت‌ها، لیست خالی از سرور اکنون معتبر تلقی می‌شود. در نتیجه اگر همهٔ نوت‌ها از سرور حذف شده باشند، cache محلی با دادهٔ قدیمی پر نمی‌ماند. عملیات آفلاین نیز بعد از دریافت snapshot روی دادهٔ سرور اعمال می‌شود تا نوت‌های در صف ارسال از رابط کاربری حذف نشوند.

### جلوگیری از بازنویسی cache با مقدار قدیمی

در ذخیرهٔ نوت، مجموعهٔ بعدی نوت‌ها قبل از نوشتن cache ساخته می‌شود. این کار مسیر ذخیره را شفاف‌تر و احتمال بازنویسی cache با closure قدیمی را کمتر می‌کند.

### rollback برای تغییرات تسک

در `TasksView` و `FolderKanban` تغییرات خوش‌بینانهٔ UI پس از خطای شبکه اکنون به مقدار قبلی برمی‌گردند. کاربر همچنان پیام خطا می‌گیرد، اما رابط کاربری وضعیت تأییدنشده را به‌عنوان وضعیت نهایی نمایش نمی‌دهد.

### بهبود ویرایشگر موبایل

نوار ابزار rich editor در نمایشگرهای کوچک اکنون در یک ردیف افقی قابل اسکرول است. بنابراین دکمه‌ها بدون ایجاد ارتفاع زیاد یا فشرده‌شدن نامناسب در دسترس می‌مانند. در نمایشگرهای بزرگ رفتار چندردیفه حفظ شده است.

### محدودکردن فولدرهای نوت جدید

فولدرهای فرم ساخت نوت اکنون فقط برای کاربر واردشده درخواست می‌شوند. این تغییر هم رفتار UI را دقیق‌تر می‌کند و هم از نمایش فولدرهای نامرتبط در صورت پیکربندی باز سرویس جلوگیری می‌کند.

## یافته‌های باقی‌مانده برای مرحلهٔ بعد

چند هشدار lint دربارهٔ وابستگی‌های `useEffect` در بخش‌های مختلف وجود دارد. این هشدارها build را متوقف نمی‌کنند، اما بهتر است در مرحلهٔ بعد با `useCallback`، وابستگی‌های کامل یا refهای کنترل‌شده اصلاح شوند؛ زیرا در برخی مسیرها می‌توانند باعث stale closure یا بارگذاری مجدد ناخواسته شوند.

رابط نوت در موبایل هنوز قابلیت‌های زیادی را هم‌زمان نمایش می‌دهد، از جمله فایل، صدا، ویدیو، لینک، قالب‌بندی و AI. پیشنهاد مرحلهٔ بعد این است که ابزارهای کم‌مصرف در یک منوی «بیشتر» قرار گیرند و سه عمل اصلی نوشتن، voice و پیوست در دسترس مستقیم باقی بمانند.

ویرایش عنوان نوت در `NotesView` برای هر تغییر، یک ذخیرهٔ فوری انجام می‌دهد، در حالی که محتوای اصلی debounce دارد. برای کاهش درخواست شبکه و جلوگیری از تداخل ذخیره‌ها، پیشنهاد می‌شود عنوان نیز مانند محتوا debounce شود و هنگام خروج از نوت یک flush نهایی انجام شود.

تقویم و Kanban در این بررسی به‌صورت ایستا و از روی کد ارزیابی شدند. برای بررسی نهایی UX، اجرای تعاملی با حساب کاربری و دادهٔ واقعی لازم است تا gestureها، drawer نوت، drag-and-drop، اشتراک‌گذاری و دسترسی آفلاین روی دستگاه واقعی سنجیده شوند.

## اعتبارسنجی

در پایان بررسی:

- `npm run lint` با کد خروجی موفق اجرا شد؛ فقط هشدارهای موجود پروژه باقی مانده‌اند.
- `npm test -- --run` با موفقیت اجرا شد: ۱۱ فایل تست و ۵۰ تست سبز.
- `npm run build` با موفقیت اجرا شد و فایل‌های PWA تولید شدند.
- `git diff --check` بدون خطا اجرا شد.

## نتیجهٔ اجرایی

نسخهٔ فعلی برای رفتن به مرحلهٔ بعد آماده است. اصلاحات اعمال‌شده کم‌ریسک و عمدتاً محافظتی هستند و مسیرهای اصلی تسک، فولدر، نوت و تقویم را تغییر اساسی نمی‌دهند. مرحلهٔ بعد باید روی کاهش شلوغی ابزارهای موبایل، debounce عنوان نوت، پاک‌سازی هشدارهای hook و تست تعاملی روی APK تمرکز کند.

## References

[1]: https://react.dev/reference/react/useEffect "React useEffect Reference"
[2]: https://tanstack.com/query/latest/docs/framework/react/overview "TanStack Query Documentation"
[3]: https://tiptap.dev/docs/editor/getting-started/overview "Tiptap Editor Documentation"
[4]: https://capacitorjs.com/docs "Capacitor Documentation"

مخزن بررسی‌شده: `hamedharami-hub/Arshiam`  
نام محصول داخل برنامه: `ARSHNAZ`

نگارش: **Manus AI**
تاریخ بررسی: ۹ سپتامبر ۲۰۲۶
