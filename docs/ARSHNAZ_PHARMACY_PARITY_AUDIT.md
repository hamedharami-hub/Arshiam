# ARSHNAZ × Pharmacy — ممیزی و وضعیت اجرای وب

آخرین بازبینی: 2026-09-25
پایهٔ Git در آغاز این برش ARSHNAZ: شاخهٔ `codex/knowledge-card-quality`، `HEAD` و `origin/main` روی `135ded92fa6c785a58e404b144d51410a388b164`
پایهٔ Pharmacy: `main` / `5b4f7d2`

مخزن Pharmacy روی `main` در `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` بررسی و بدون تغییر باقی ماند. در checkout ARSHNAZ، یک تغییر از قبل موجود و نامرتبط در `scripts/buildCompletePharmacySeedData.mjs` حفظ شده و در این اصلاح staged یا منتشر نشده است.

این سند تغییرهای کد/داده و شکاف‌های باقی‌مانده را ثبت می‌کند. در اجرای QA ثبت‌شده در این گزارش، هیچ داده‌ای در Firestore زنده وارد یا ویرایش نشد و APK ساخته نشد.

Workflowهای Android debug و signed اکنون فقط با اجرای دستی شروع می‌شوند؛ pushهای وب روی `main` فقط CI وب و استقرار Vercel را فعال می‌کنند و APK ناخواسته نمی‌سازند.

## تازه‌سازی و ممیزی ساختاریِ این بازبینی

- snapshot فعلی همچنان ۴۳۲ سند، ۳۴ پوشه و ۳۵ کارت دارد؛ شناسه‌ها یکتا هستند و هیچ ارجاع پوشهٔ والد، پوشهٔ سند، یا سند/پوشهٔ کارت شکسته نیست.
- شمارش مستقلِ متن دو زبانه پس از renderer انگلیسیِ محلی ۸۰۸ پیوند داخلی را تأیید کرد: ۵۳۴ در HTML فارسی و ۲۷۴ در انگلیسی؛ همگی به شناسهٔ سند موجود اشاره می‌کنند.
- هر ۴۳۲ `source_url` به همان SHA ثابت Pharmacy یعنی `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` اشاره می‌کند؛ این SHA در `origin/main` مخزن Pharmacy نیز همان commit فعلی است. این، تطبیق منشأ داده را ثابت می‌کند، نه صحت علمی/بالینی را.
- هر ۴۳۲ سند هنوز `content_review_status: unreviewed` دارد. این وضعیت تا بازبینی واجدصلاحیت و ثبت مرجع معتبر، حوزهٔ قضایی و تاریخ نباید به «بازبینی‌شده» ارتقا پیدا کند.
- تست importer حالا یکتایی شناسهٔ پوشه/کارت، اعتبار والد پوشه‌ها، یکنواختی SHA مبدأ و حفظ تعداد هر ۸۰۸ پیوند را هم می‌سنجد. اجرای متمرکز آخر: ۱۴/۱۴ موفق؛ timeout دو تست حجیم به ۱۵ ثانیه افزایش یافته است.
- GitHub برای commit `e39549f` وضعیت Vercel را `success` گزارش کرد و مسیر `/app/interactive-study` در دامنهٔ زنده بارگذاری می‌شود؛ API متصل Vercel جزئیات deployment را با 404 برگرداند، بنابراین نگاشت دقیق alias دامنه به شناسهٔ deployment مستقلاً تأیید نشده است.
- renderer محلیِ ۲۸ سناریوی بالینی/اصطلاحی اکنون بخش‌های سناریو، سؤال‌ها و پاسخ‌ها، red flagها، منطق و ارجاع را در متن انگلیسی نیز بازمی‌گرداند؛ آزمون seed نبود نویسهٔ فارسی/عربی و وجود متن انگلیسی را برای همهٔ ۲۸ مورد کنترل می‌کند. این بهبود ترجمه/نمایش است و صحت علمی محتوا را تأیید نمی‌کند.
- در QA و اصلاح این برش هیچ داده‌ای در Firestore زنده وارد/ویرایش نشد. override موضعی UTI فقط در مسیر import/ارتقای ایمن seed اعمال می‌شود و وضعیت سند را تأییدشده نمی‌کند. ممیزی علمی ۴۳۱ سند دیگر همچنان منبع‌به‌منبع و نیازمند بازبینی انسانی واجدصلاحیت است.

## برش ممیزی ایمنی بالینی — UTI/Cystitis

- سند `doc-disease-uti_cystitis` در Pharmacy با برچسب `unreviewed` وارد شده بود، اما Ural را «First-line OTC Pharmacotherapy» می‌نامید، تسکین قطعی ظرف ۳۰ دقیقه را ادعا می‌کرد، حجم ثابت ۲–۲٫۵ لیتر را برای «شست‌وشوی مکانیکی» توصیه می‌کرد و مجوز داروساز را به چهار ایالت تعمیم می‌داد.
- منابع رسمی بررسی‌شده در ۲۵ سپتامبر ۲۰۲۶: استاندارد NSW Health مورخ ۱۵ ژوئن ۲۰۲۶ می‌گوید اثربخشی قلیایی‌کننده‌ها برای تسکین علامتی UTI ثابت نشده، هرچند برخی بیماران آن‌ها را مفید می‌دانند؛ این مواد اثر ضدمیکروبی nitrofurantoin را به‌طور قابل‌توجهی کم می‌کنند. ویرایش ۱۲ (۲۰۲۵) Queensland Primary Clinical Care Manual ایمنی/اثربخشی را نامعلوم می‌داند و برای cystitis نوشیدن به‌اندازهٔ رفع تشنگی را توصیه می‌کند. دستور سازندهٔ Ural برای افراد ۱۲ سال به بالا ۱–۲ ساشه تا ۴ بار در روز است. پروتکل ویکتوریا در ژانویهٔ ۲۰۲۶ هم نمونه‌ای از اختیار محدود، آموزش‌محور و وابسته به برنامه/حوزهٔ قضایی است؛ این اختیار را نمی‌توان به کل استرالیا تعمیم داد.
- در مسیر seed importer یک اصلاح تحریریهٔ دوزبانه اضافه شد: عنوان خط اول به «تسکین علامتی، نه درمان خط اول» تغییر می‌کند؛ ادعای زمان اثر و توصیهٔ حجم ثابت آب حذف می‌شوند؛ دستور سازنده، منع همراهی با nitrofurantoin و یادداشت حوزه‌ای با پیوند مستقیم به منابع رسمی نشان داده می‌شود.
- شناسهٔ سند، پوشه، تگ‌ها، منبع Pharmacy، پیوندهای داخلی و وضعیت `unreviewed` حفظ می‌شوند. اصلاح فقط هنگام import جدید یا وقتی سند سرور دقیقاً با نسخهٔ قدیمی seed/legacy برابر باشد قابل اعمال است؛ هر نسخهٔ دست‌کاری‌شدهٔ کاربر مستثنا می‌ماند. مسیر بررسی وضعیت هم محتوای صدها سند را بی‌جهت sanitize نمی‌کند.
- اعتبارسنجی این برش: تست‌های `pharmacyImportService`، ۱۶/۱۶؛ typecheck؛ ESLint فایل‌های تغییرکرده؛ build تولیدی موفق. build همچنان هشدارهای bundle حجیم Pharmacy و precache موجود را نشان می‌دهد؛ این‌ها در این برش اصلاح نشدند.
- این اصلاح موضعی به معنی بازبینی بالینی واجدصلاحیت یا تأیید کل سند نیست. هیچ رکورد Firestore زنده تغییر نکرده؛ سایر دوزها، Hiprex، معیارهای تریاژ و همهٔ اسناد دیگر همچنان به بازبینی منبع‌به‌منبع و انسانی نیاز دارند.

## اصلاح‌های محلی انجام‌شده

- در تولید آموزش تعاملی، شکست یا پاسخ خالی AI دیگر به محتوای نمونهٔ ثابت و نامرتبطِ بالینی تبدیل نمی‌شود؛ همچنین خروجیِ فاقد کنترل‌های لازم برای قالب انتخاب‌شده رد می‌شود و چیزی به درس افزوده نمی‌شود. تست پاسخ معتبر، کنترلِ ایستا، خطای سرویس و پاسخ خالی اضافه شد. تابع قدیمی مولد نمونه‌ها فقط با برچسب deprecated باقی مانده و از مسیر اجرایی استفاده نمی‌شود. Vitest کامل: ۸۵ فایل و ۴۸۹ تست موفق؛ typecheck و build موفق؛ lint بدون خطا و با ۵۹ هشدار موجود.
- توضیح گزینهٔ آزمون که از markup تولیدشده/ذخیره‌شده می‌آید دیگر هنگام نمایش با `innerHTML` تفسیر نمی‌شود؛ با `textContent` به متن امن تبدیل شد. تست payload شبیه `<img onerror>` تأیید می‌کند عنصر اجرایی به DOM تزریق نمی‌شود و متن همچنان خواناست.
- مولد دادهٔ Pharmacy دیگر فقط مکانیزم‌های کلی را برنمی‌گزیند؛ ۷۰ سند مکانیزم اختصاصی کلاس دارویی را هم با شناسه‌های مورد انتظار لینک‌های محصولات می‌سازد.
- اکنون seed شامل ۴۳۲ سند، ۳۴ پوشه و ۳۵ کارت است. بررسی ایستای فایل ساخته‌شده ۴۳۲ شناسهٔ یکتا، ۷۹۲ پیوند داخلی و صفر پیوند شکسته نشان داد.
- برای تمام ۴۳۲ سند، `source_url` نسخه‌ثابت‌شده به فایل دادهٔ متناظر در مخزن Pharmacy تولید می‌شود. این پیوند «ردیابی منشأ در مخزن» است، نه مدرک تأیید مستقل بالینی.
- اسناد seed با وضعیت `content_review_status: unreviewed` علامت می‌خورند و خوانندهٔ دانش این هشدار را نشان می‌دهد؛ وضعیت باید تا ثبت بازبینی واجدصلاحیت باقی بماند.
- واردکردن نسخهٔ تازه، رکورد قدیمی را فقط وقتی به‌روزرسانی می‌کند که هنوز دقیقاً با نسخهٔ قدیمی seed برابر باشد. لینک منبع شخصی نیز در تشخیص «ویرایش‌نشده» لحاظ می‌شود تا لینک سفارشی کاربر بازنویسی نشود؛ محتوای ویرایش‌شده و پیشرفت مطالعه حفظ می‌شود.
- حذف فولدر حالا پیش از حذف، اسناد و زیرفولدرهای مستقیم را در والد/ریشه نگه می‌دارد؛ تغییرهای مرتبط ابتدا در Firestore یا صف پایدار آفلاین پذیرفته می‌شوند و سپس cache به‌روز می‌شود. رابط هم تأیید حذف و توضیح حفظ محتوا دارد.
- صف آفلاین هنگام بازگشت IndexedDB، رکوردهای fallback موجود در `localStorage` را هم می‌خواند و ارسال می‌کند؛ قبلاً مسیر IndexedDB می‌توانست آن تغییرها را پنهان کند. پاک‌کردن/به‌روزرسانی آیتم fallback نیز با تأیید نوشتن انجام می‌شود.
- دکمهٔ منوی فولدرها نام دسترس‌پذیر فارسی/انگلیسی دارد.
- شمارندهٔ پوشه در درخت دانش، تعداد اسناد کل زیرشاخه را نمایش می‌دهد، نه فقط اسناد مستقیم همان پوشه.
- فیلترهای دانش، گروه‌های Schedule 2/3/4/8 را برای مرور مونوگراف‌های محصول نشان می‌دهند؛ این‌ها فقط فیلتر دسته‌بندی‌اند و توصیهٔ عرضه/درمان نیستند.
- نمایش HTML محتوای دانش و خروجی تحلیل هوش مصنوعی با DOMPurify پاک‌سازی می‌شود؛ data-attributeهای مجازِ لینک‌ها و تمرین‌های تعاملی نگه داشته می‌شوند.
- فایل‌های seed بزرگ Pharmacy از precache اولیهٔ PWA کنار گذاشته شدند؛ چون واردکردنشان فقط با اتصال آنلاینِ تأییدشده ممکن است، بارگیری‌شان هنگام نیاز انجام می‌شود.
- `@huggingface/transformers` به `4.3.0` و `react-router-dom` به `7.18.4` ارتقا یافتند. حذف `firebase-admin` به‌دلیل استفادهٔ API اشتباه بود و پیش از انتشار برگردانده شد.
- ممیزی مرور لایتنر یک مسیر دو بار امتیازدهی در کلیک سریع یافت؛ قفل حین ذخیره و حالت disabled برای دکمه‌ها اضافه شد. سرویس هم حالا ویرایش کارت با پرسش/پاسخ خالی را رد می‌کند.
- تست‌های importer به پوشش ۴۳۲ سند، ۱۴ نمای کلی و ۷۰ مکانیسم اختصاصی کلاس، URLهای منبع، عنوان CYP2D6 و حفظ لینک منبع سفارشی گسترش یافتند.
- هشدار reader برای لینک‌های سفارشی، برخلاف لینک‌های repository Pharmacy، آن‌ها را به‌درستی «فایل مبدأ Pharmacy» برچسب نمی‌زند.

## شواهد اعتبارسنجی

- آخرین اجرای `npm run build` موفق شد و typecheck داخل build نیز موفق بود؛ Vite هشدار chunkهای بزرگ و importهای هم‌زمان static/dynamic را داد.
- حذف اولیهٔ `firebase-admin` اشتباه بود: کد API به آن وابسته است و یک suite تست resolve نمی‌شد؛ وابستگی به manifest و lockfile برگردانده شد. نصب پاک با `npm ci --dry-run` و suite مربوط به API بعد از اصلاح موفق شدند.
- بررسی ایستای seed: صفر شناسهٔ تکراری، صفر پیوند شکسته و صفر سند بدون `source_url`.
- پس از اصلاح وابستگی، اجرای کامل Vitest موفق بود: ۸۳ فایل تست، ۴۵۸ تست.
- `npm run lint` موفق شد (۰ خطا، ۶۰ هشدار lint موجود در پروژه)؛ `npm run build` و typecheck نیز موفق شدند. اجرای build هنوز هشدارهای chunkهای بزرگ و importهای هم‌زمان static/dynamic دارد.
- `npm audit --omit=dev` اکنون ۸ آسیب‌پذیری Moderate در زنجیرهٔ `firebase-admin`/`uuid` نشان می‌دهد؛ رفع خودکار پیشنهادی به ارتقای breaking به `firebase-admin@14.5.0` نیاز دارد و در این اصلاح انجام نشد. audit کامل ۱۴ مورد (۱۳ Moderate، ۱ High) نشان می‌دهد؛ High در toolchain Vite است.
- اعتبارسنجی پس از اصلاح درج امن توضیح کوییز: Vitest کامل ۹۳ فایل/۵۵۱ تست، typecheck و ESLint فایل‌های تغییرکرده موفق شدند؛ build هم موفق شد. هشدارهای قبلی Vite دربارهٔ chunkهای بزرگ و importهای ایستا/پویا باقی است.
- `git diff --check` موفق بود؛ فقط هشدارهای line-ending ویندوز گزارش شد.
- chunk دادهٔ Pharmacy همچنان حدود ۳٫۶ MB پیش از فشرده‌سازی (حدود ۴۱۴ KB gzip) است، اما اکنون هنگام نیاز بارگیری می‌شود و در precache نیست. حجم precache با ۱۵۷ فایل حدود ۵٫۹ MB است (کاهش حدود ۴٫۵ MB). چند هشدار قدیمی Vite دربارهٔ importهای هم‌زمان static/dynamic نیز باقی است.
- `npm audit` کامل هنوز ۷ مورد در ابزارهای توسعه گزارش می‌کند: ۶ Moderate و ۱ High. مورد High به Vite و رفع پیشنهادی آن ارتقای major به Vite 8 است؛ این ارتقا در این مرحله برای جلوگیری از مهاجرت پرریسک toolchain انجام نشد. این‌ها در گزارش production-only audit دیده نمی‌شوند.

## موارد باز و مهم

1. کد ممیزی‌شده روی `main` و Vercel production منتشر شده است، اما Firestore زنده همچنان محتوای قدیمی CYP1A2 را نشان می‌دهد و خود صفحه هشدار می‌دهد ترجمهٔ فارسیِ متن ناقص است. انتشار کد به‌تنهایی دادهٔ حساب را وارد یا بازنویسی نمی‌کند.
2. سندهای Pharmacy فعلاً عمدتاً محتوای دانشی/ایستا هستند. تجربه‌های تعاملی Pharmacy—مصاحبه و triage OTC، جست‌وجوی ساختاریافتهٔ قفسهٔ دارو و شبیه‌سازی گردش کار FRED—هنوز به‌صورت workflowهای واقعی در ARSHNAZ پیاده‌سازی نشده‌اند.
3. ثبت لینک به فایل کد، منشأ داده را قابل ردیابی می‌کند اما جایگزین منبع اولیهٔ بالینی نیست. در schemaهای دادهٔ Pharmacy فیلدهای ساختاریافتهٔ `sourceUrl`، `reviewedAt`، `reviewBy`، `accessedAt` و `jurisdiction` پیدا نشدند؛ `ClinicalRelation.source/reason` توضیح منشأ/منطق داخلی رابطه است، نه citation بالینی. هر ادعای درمانی/دوز/تداخل/قانون باید به منبع جاری استرالیا، حوزهٔ قضایی، تاریخ بررسی و بازبین بالینی متصل شود. دادهٔ Pharmacy بدون این فرایند نباید «تأیید بالینی» تلقی شود.
4. گراف روابط نوع‌دار Pharmacy به‌صورت دادهٔ ساختاریافته منتقل، روی `main` منتشر و در شبکهٔ روابط قابل نمایش شده است؛ با این حال هیچ داده‌ای به Firestore زنده نوشته نشده و نمایش رکوردهای seed نیازمند import جداگانه و صریح است.
5. نتیجهٔ audit کامل وابستگی‌ها باید پیش از اعلام «آمادهٔ استفادهٔ نهایی» دوباره بررسی شود؛ major upgrade ابزار ساخت یک کار جدا و نیازمند اعتبارسنجی است.
6. ریسک بازگشت cache قدیمی اصلاح شد: پس از دریافت موفق remote، همان پاسخ مبناست و فقط mutationهای pending همان حساب و collection روی آن replay می‌شوند. این قاعده برای پوشه‌ها، اسناد، کارت‌های Leitner و پیوندهای تسک-دانش اعمال شده و تست رگرسیون cache-only deletion، insert/update/delete آفلاین و جداسازی حساب دارد.
7. جست‌وجوی نقشهٔ ذهنی اکنون نام پوشه، عنوان فارسی/انگلیسی سند، متن، tag، سؤال/پاسخ/clue کارت و تفاوت نویسه‌های عربی/فارسی را پوشش می‌دهد؛ تمام والدهای چندسطحی و سند والد کارت برای نمایان‌شدن نتیجه باز می‌شوند و این مسیر تست رگرسیون مستقل دارد.
8. **بازبینی انسانیِ سناریوی SafeScript همچنان لازم است:** ادعاهای قبلی دربارهٔ شمارهٔ رویداد پلیس و منع قانونیِ خودکار در seed فعلی حذف شده‌اند؛ تست regression عبارت‌های قطعی/نادرست را رد و ارجاع‌های NSW Health دربارهٔ alert و نسخهٔ گم‌شده را کنترل می‌کند. این اصلاح و منبع‌دهی، محتوای آموزشی را به `reviewed` ارتقا نمی‌دهد؛ تا بازبینی واجدصلاحیت، نسخهٔ فعلی نیز توصیهٔ عملی یا راهنمای عرضه محسوب نمی‌شود. [راهنمای NSW Health دربارهٔ alertهای SafeScript](https://www.health.nsw.gov.au/pharmaceutical/safescript/practitioners/Pages/pop-up-notifications-and-alerts.aspx)، [راهنمای NSW دربارهٔ نسخه‌های گم‌شده/سرقت‌شده](https://www.health.nsw.gov.au/pharmaceutical/Pages/lost-stolen-prescriptions.aspx).

## گام‌های بعدی پیشنهادی

1. بررسی بصری نسخهٔ محلی و آزمون عملی در browser؛ تست‌های UI صفحه محدودند و سناریوهای کامل reader، منوها و تعامل‌های واقعی هنوز آزمون خودکار ندارند.
2. پس از تأیید صریح برای محیط زنده، اجرای importer امن روی حساب Firestore هدف؛ ابتدا فقط شمارش missing/upgradeable نمایش داده شود و نتیجهٔ server-side تأیید گردد. محتوای ویرایش‌شده و کارت‌های موجود نباید overwrite شوند.
3. پیش از import زنده، پیش‌نمایش شمارش missing/upgradeable و تأیید عدم overwrite محتوای شخصی؛ انتشار وب کد طبق دستور حامد از شاخهٔ `main` انجام می‌شود.
4. توسعهٔ parity به‌ترتیب: کاتالوگ محصولات و فیلتر ساختاریافته؛ ابزارهای آموزشی OTC با مرز روشن عدم‌تشخیص؛ سپس شبیه‌ساز dispensing آموزشی. هر محتوای بالینی/حقوقی قبل از استفادهٔ عملی نیازمند source review و بازبین واجد صلاحیت است.

نسخهٔ اندروید عمداً ساخته نشده است.

## برنامهٔ ادامهٔ ممیزی و همسان‌سازی (درخواست ۲۵ سپتامبر)

این موارد مرحله‌به‌مرحله بررسی و اجرا می‌شوند. وضعیت «نیازمند آزمون» به معنی تأیید عملکرد نیست. دادهٔ زندهٔ Firestore و نسخهٔ اندروید در این برنامه تغییر نمی‌کنند مگر با تصمیم جداگانهٔ حامد.

1. **خواندن و ناوبری دانش:** نمایش متن فارسی/انگلیسی، لینک‌های درون متن و «مرتبط»، رفتن به دارو/بیماری هدف، بازگشت مرورگر و دکمهٔ بازگشت به سند قبلی، حفظ حالت و جایگاه خواندن؛ آزمون روی موبایل و نمایشگر بزرگ. باگ تغییرنکردن URL و بازنگشتن به سند قبلی اصلاح شد؛ آزمون read-only production در ۲۵ سپتامبر ثابت کرد انتخاب سند، `docId` را عوض و دکمهٔ «Back to previous document» سند قبلی را بازیابی می‌کند. بازگشت native مرورگر، جایگاه اسکرول و دستگاه‌های دیگر هنوز باید جداگانه آزموده شوند.
2. **پرسش و Leitner:** مسیر «انتخاب متن یا checkpoint → تولید سؤال با AI/دستی → پیش‌نمایش قابل‌ویرایش → ذخیرهٔ پایدار کارت → مرور → بازگشت به منبع» با حالت‌های شکست، سؤال تکراری و تولید بی‌ربط آزموده شود. تنظیمات فاصلهٔ مرور، امتیازدهی، موعد، دسته‌بندی و نمای مرور با Pharmacy مقایسه شود؛ هر حالت موجود در Pharmacy که ارزش آموزشی دارد با معیار پذیرش مشخص منتقل شود. تولید AI بدون منبع‌سنجی به معنی درستی علمی نیست.
3. **نقشهٔ ذهنی:** حالت‌های نمایش و مطالعهٔ شاخه‌ها، فیلترها، بازکردن مسیر والد، جابه‌جایی سند/کارت و حفظ وضعیت با Pharmacy مقایسه و نواقص عملکردی جداگانه ثبت شود.
4. **زیر‌دسته‌های بیماری و دارو:** ساختار دسته‌بندی Pharmacy و نیازهای جست‌وجوی ARSHNAZ تطبیق داده شود؛ دسته‌های بیشتر تنها با شناسهٔ پایدار، رابطهٔ والد/فرزند روشن، بدون جابه‌جایی یا overwrite محتوای شخصی و با مهاجرت قابل‌آزمون اضافه شوند. متن درمانی تازه بدون منبع بالینی معتبر ساخته نشود.
5. **آموزش تعاملی مستقل:** یک ورودی و صفحهٔ مجزا برای تجربه‌های تعاملی Pharmacy طراحی شود. منبع مقایسه شامل `OtcTriageModule`، `DiseaseTriagePanel`، سناریوها/گام‌های triage و شبیه‌ساز `FredDispenseModule` است. جریان سناریو، انتخاب پاسخ، بازخورد، هشدارهای red flag و امکان بازشروع باید واقعاً تعاملی بماند، نه اینکه صرفاً متن ثابت منتقل شود. برای این بخش کپی متن یا انتقال به Leitner لازم نیست. تا بررسی علمی و حقوقی، برچسب «آموزشی، نه توصیهٔ درمانی یا مجوز عرضه» حفظ شود.
6. **اعتبار علمی و پذیرش نهایی:** هر ادعای حساس دارویی/دوز/تداخل/قانون با منبع معتبر، تاریخ، حوزهٔ قضایی و بازبین واجدصلاحیت بررسی شود. برای هر مرحله تست واحد/تعامل، تست ذخیره و بازگشت، typecheck، build و آزمون بصری production انجام شود. ابتدا وب؛ APK طبق درخواست فعلی ساخته نشود.

### یافتهٔ تازهٔ ممیزی تولید کارت

- در production، پنجرهٔ Generate Cards برای سند CYP2C19 دو کارت از پیش آماده نشان داد؛ پرسش «هشدار یا منع مصرف مهم چیست؟» داشت، ولی پاسخ آن کل متن ۷۳۷ کاراکتریِ سند بود، نه پاسخ مستقیم به پرسش. متن شامل `&amp;` خام هم بود. این مشاهده نشان‌دهندهٔ ضعف کیفیت/قالب‌بندی خروجی پیش‌نمایش است؛ صحت تولید جدید با AI و ذخیرهٔ کارت هنوز آزموده نشده و نباید تأییدشده تلقی شود. پیش از ذخیرهٔ خودکار، پاسخ باید کوتاه، مستند به بخش مشخص متن و قابل‌ویرایش باشد و entityهای HTML decode شوند.
- اصلاح کیفیت سؤال AI در commitهای `88943a7` و `cdfab72` روی `main` منتشر شد: پاسخ برابر/بخش عمدهٔ متن منبع رد می‌شود؛ استخراج‌گر آفلاین برچسب‌های فارسی/انگلیسی را جدا می‌کند، پاسخ تکراری را حذف و entityهای HTML را decode می‌کند. این انتشار ثابت نمی‌کند خروجی تازهٔ AI از نظر علمی درست است؛ آزمون تولید و ذخیرهٔ زنده همچنان انجام نشده است.
- اصلاح ذخیرهٔ لایتنر در commit `0ef400a` روی `main` منتشر شد: شکست outbox پایدار دیگر موفقیت کاذب نمی‌دهد، کارت cache-only حذف می‌شود و خطای ذخیرهٔ دسته‌ای پیش‌نمایش کارت‌های ذخیره‌شده را برای retry نگه نمی‌دارد. تست‌های مرتبط آن دور موفق بودند؛ ذخیرهٔ زنده در حساب کاربر آزمایش نشده است.

### تطبیق ایستای حالت‌های مرور در Pharmacy

- روی آخرین `pharmacy/main` در fetch روز ۲۵ سپتامبر (commit `5b4f7d2`؛ remote جلوتر نبود)، مرور دارای due/cram، فیلترهای domain/system/subsystem/box/single-card، ترتیب کارت، تمِ زبان فارسی/انگلیسی/دوزبانه، تایمر، Zen mode و پنل تنظیمات با تب‌های study/FSRS/display/advanced/backup است. Mind map علاوه بر دو شکل/نمای کلی، انتخاب دیدگاه بالینی، Drill Branch، نشان‌گذاری و خروجی شاخه دارد. commit تازه‌تر این شاخه همچنین modalهای portal/bounded و قفل اسکرول، مسیر بازگشت از Fred/سناریو، و `useDeferredValue` برای جست‌وجوی سنگین را اضافه کرده است. Reader/Study modals در ARSHNAZ از Dialog مبتنی بر Radix و Portal استفاده می‌کنند؛ کپی‌کردن portal دستی Pharmacy لازم نیست. این بررسی ایستا است؛ آزمون بصری تک‌تک حالت‌ها انجام نشده است.
- یک شکاف عملکردی در خود Pharmacy پیدا شد: `LearningToolsModule`، `LeitnerMindMapPanel` را بدون `onRateCard` و `onUpdateCardBox` رندر می‌کند؛ `BranchStudyRunnerModal` همچنان دکمه‌های Again/Hard/Good/Easy را نشان می‌دهد و فقط وقتی یکی از callbackها برسد زمان‌بندی کارت را تغییر می‌دهد. در این اتصال، امتیاز فقط در state موقت مودال می‌ماند و زمان مرور/جعبه ذخیره نمی‌شود. این یافته از مسیرهای prop در کد است و به‌عنوان رفتار production Pharmacy بازآزمایی نشده است. ARSHNAZ هنگام پیاده‌سازی مرور شاخه باید همان سرویس پایدار Leitner را صدا بزند و نتیجهٔ ذخیره را گزارش کند؛ کپی ظاهر دکمه به‌تنهایی parity نیست.
- ورودی تعاملی OTC در Pharmacy سه mode سناریو دارد (اداری/روتین، OTC/عامیانه، تعارض/اخلاق)، کاتالوگ بیماری با جست‌وجو و زیردسته، فیلتر نخوانده/دارای سناریو، شبیه‌ساز گفتگو، WWHAM، Red Flags، انتخاب تصمیم داروساز و بازخورد خروجی؛ پنل بیماری هم سناریوهای مرتبط را مستقیم باز می‌کند. آموزش گردش کار FRED ماژول جداگانه با چند نوع نسخه، command/shortcut، محاسبه و check ایمنی، final check و handout است. این قابلیت‌ها بخش‌های مستقل hub آموزشی پیشنهادی‌اند؛ دادهٔ بیمار/نسخه باید ساختگی و آموزشی باشد و محتوای حساس حقوقی با منبع روز استرالیا تطبیق بخورد.

### نیازمندی تازهٔ الگوهای نمایش مطالعه در Leitner و Mind Map

- **ممیزی مرجع پیش از طراحی:** همهٔ حالت‌های نمایش/مطالعهٔ کارت و Mind Map در Pharmacy باید در UI خود برنامه، علاوه بر کد، یک‌به‌یک فهرست و عملاً آزموده شوند؛ مواردی مثل مرور due و cram، مرور تک‌کارت/شاخه، چیدمان‌ها و دیدگاه‌های نقشه، فیلترها و ترتیب، انتخاب فارسی/انگلیسی/دوزبانه، تایمر و Zen، نشان‌گذاری و خروجی شاخه بررسی شوند. برای هر حالت تصویر/عرض نمایش، مسیر ورود و خروج، کنترل‌ها، وضعیت خالی/خطا و نتیجهٔ تعامل ثبت شود؛ فهرست ایستای فعلی به‌تنهایی اثبات نمی‌کند که حالت در اجرا درست کار می‌کند.
- **سنجش ارزش آموزشی و انتخاب:** برای هر الگو مشخص شود چه چیزی خواندن را آسان‌تر می‌کند (تمرکز روی یک کارت، دیدن رابطهٔ شاخه‌ها، انتخاب محدودهٔ کوچک، جهت/زبان نمایش) و آیا محتوا در صفحهٔ بلند یا تب‌های دور از دسترس پنهان می‌شود. سپس الگوهای واقعاً مفید برای کاربر انتخاب شوند؛ هدف انتقال کورکورانهٔ همهٔ گزینه‌ها یا پیچیده‌کردن صفحه نیست.
- **پایداری عملکردی، نه صرفاً ظاهر:** برای هر حالت مسیر انتخاب مجموعه/پوشه/زیرفولدر → نمایش آیتم‌های درست → امتیاز پاسخ یا ثبت مطالعه → به‌روزرسانی جعبه/موعد بعدی → refresh/بازگشت/همگام‌سازی بررسی شود. به‌طور خاص شکاف شناخته‌شدهٔ Pharmacy در `onRateCard` و `onUpdateCardBox` باید مرجعِ «رفتار معیوب که نباید کپی شود» باشد؛ ARSHNAZ باید از سرویس پایدار Leitner خودش استفاده کند و فقط پس از تأیید ذخیره، پیشرفت یا موفقیت نشان دهد.
- **پذیرش UI و آزمون رگرسیون:** حالت‌های منتخب با ظاهر یکپارچه و خلوت پیاده شوند، روی موبایل، پنل باریک و صفحهٔ عریض/تاشو بدون برش، اسکرول ناخواسته یا کنترل غیرقابل‌دسترسی کار کنند. تست باید با دادهٔ ساختگی ثابت کند هر حالت و فیلتر مجموعهٔ درست را نشان می‌دهد، انتخاب زبان/نمایش بعد از بازگشت حفظ می‌شود، امتیاز و موعد بعدی واقعاً ذخیره می‌شوند و مرور نقشهٔ ذهنی به همان شاخه برمی‌گردد؛ هر حالتِ فاقد رفتار پایدار تا زمان اصلاح، منتشر نشود.

### نیازمندی‌های UX تازهٔ Knowledge (درخواست ۲۵ سپتامبر)

1. **زبان و متن دوزبانه:** زبان پیش‌فرض صفحه انگلیسی باشد و کنترل کوچک سه‌حالته بین English / فارسی / دوزبانه جابه‌جا شود. تیتر، محتوای دارو/بیماری، checkpoint و هر کارت تولیدشده باید در هر دو زبان قابل‌خواندن باشد؛ در حالت دوزبانه، پرسش و پاسخ هرکدام جفت فارسی/انگلیسی داشته باشند. نمایش ناقص ترجمه یا جایگزین‌کردن پاسخ یک‌زبانه با کل متن منبع پذیرفته نیست.
2. **چیدمان ترکیبی مطالعه:** محتوای اصلی نباید پشت زیرتب‌های عمودی/کشویی پنهان یا برای کشف آن نیازمند پیمایش طولانی باشد. بخش‌های مطالعه و انتخاب کارت باید با تب‌ها/چیپ‌های افقی قابل انتخاب باشند و محتوای بلند در بدنهٔ عمودیِ قابل پیمایش بماند؛ دو الگو باید مکمل هم باشند و در صفحهٔ کوچک هم افقی قابل پیمایش/انتخاب بمانند.
3. **کارت AI و انتخاب زبان مرور:** خروجی AI بر اساس نوع مطلب ساختار بگیرد (عنوان/پرسش/پاسخ/نکتهٔ کوتاه، نشانهٔ تصویری فقط وقتی معنا را روشن می‌کند)، و فارسی و انگلیسی برای همان سؤال به شکل جفت ذخیره شوند. انتخاب زبان نمایش باید در مرور Leitner و Mind Map در دسترس باشد و با refresh حفظ شود. این نیاز مستلزم بررسی schema است؛ فیلدهای فعلی front/back نباید با تغییر بی‌مهاجرت شکسته شوند.
4. **پیوند درون‌متنی:** با کلیک روی بیماری/دارو/سناریوی triage، پنجرهٔ بزرگ روی همان سند باز شود و محتوای زمینه‌ای که کاربر می‌خواند پشت آن باقی بماند. دکمهٔ Back باید overlay را ببندد و دقیقاً به همان سند/موقعیت مطالعه برگردد؛ لینک گمشده یا انتقال تمام‌صفحه به صفحهٔ نامرتبط خطاست.
5. **نوار ابزار Reader:** کنترل زبان کوچک و پیش‌فرض انگلیسی؛ Generate فقط آیکون AI، Interactive فقط آیکون بازی، Study Task فقط آیکون تقویم، و یک حالت Reader. Original HTML، Copy Content و Open in external browser از رابط Knowledge حذف شوند. کنترل اندازهٔ فونت باید واقعاً متن را تغییر دهد. Edit فقط وقتی نگه داشته شود که متن محتوای اصلی را مستقیم و پایدار ویرایش کند؛ ابزارهای زینتیِ ویرایش حذف شوند و AI پس از ویرایش، پیشنهاد تمیزکاری قابل‌بررسی بدهد.
6. **معیار پذیرش UX:** آزمون فارسی، انگلیسی و دوزبانه روی عرض موبایل/پنل و صفحهٔ عریض؛ زبان بعد از ناوبری/refresh ثابت؛ تب‌های افقی با صفحه‌خوان و کیبورد قابل‌دسترسی؛ فونت در Reader تغییرقابل‌مشاهده؛ باز/بستن لینک داخلی بدون گم‌شدن سند یا scroll؛ هر کارت دوزبانه با انتخاب زبان در Leitner و Mind Map.

### نیازمندی تازهٔ «مرورها» در تسک‌ها (درخواست ۲۵ سپتامبر)

- یک دسته/نمای مستقل با نام «مرورها» برای کارهای یادگیری ساخته شود تا با کارهای عادی و وظایف شخصی مخلوط نشود. کاربر بتواند یک سند، دارو/بیماری، نقشهٔ ذهنی، فصل یا بخش مشخص را به‌عنوان موضوع مطالعه انتخاب کند و روش مرور (از جمله Leitner و روش‌های فاصله‌گذاری مبتنی بر یادآوری) را تعیین کند.
- برای موضوع انتخاب‌شده، موعدهای مطالعه/یادآوری بر اساس روش مرور و الگوریتم زمان‌بندی محاسبه شوند. مرور سررسیدشده در صفحهٔ مطالعه و همچنین در Today و ویجت‌ها به‌صورت قابل‌تشخیص نمایش داده شود؛ نمایش در Today/ویجت به معنی تبدیل یا کپی آن به تسک عادی نباشد و تداخل با برنامهٔ روزانه ایجاد نکند.
- اقدام «مرور انجام شد» باید به‌صورت پایدار ثبت شود و موعد بعدی را طبق الگوریتمِ انتخاب‌شده بسازد. تیک‌زدن نباید صرفاً کارت/تسک را ناپدید کند؛ در صورت شکست ذخیره، رابط نباید موفقیت کاذب نشان دهد و باید امکان تلاش دوباره بدهد.
- تعریف معیار پذیرش شامل جداسازی داده و فیلترها از تسک عادی، انتخاب منبع/بخش، موعد امروز و آینده، ثبت نتیجه، محاسبه و نمایش موعد بعدی، همگام‌سازی بین وب و ویجت، رفتار آفلاین/همگام‌سازی، و حفظ زمان‌بندی پس از refresh و ورود دوباره است. پیش از پیاده‌سازی باید روشن شود آیا از مدل کارت‌های Leitner فعلی استفاده می‌شود یا مرور سند/نقشه به موجودیت مطالعهٔ جداگانه نیاز دارد؛ migration باید بدون overwrite داده‌های فعلی انجام شود.
- **تسک آموزشی روزانهٔ Leitner:** کارت‌هایی که موعدشان رسیده در Today و ویجت به شکل یک تسک آموزشی با عنوانی مانند «مرور Leitner» بیایند؛ کلیک فقط جلسهٔ همان روز و کارت‌های سررسیدشدهٔ مجموعهٔ انتخابی را باز کند، نه تمام کارت‌ها را یک‌جا. بتوان پوشه و زیرفولدر، مجموعه/موضوع (مثلاً تاریخ، ریاضی، داروسازی یا همهٔ مجموعه‌ها) و شیوهٔ مطالعه را برای جلسه انتخاب کرد؛ تفکیک کارت‌ها و فیلترها حفظ شود.
- **نقشهٔ ذهنی به‌عنوان برنامهٔ مطالعه:** هر نقشهٔ ذهنی یا شاخه/سرفصل منتخب بتواند یک موضوع مطالعهٔ زمان‌بندی‌پذیر باشد؛ در تاریخ‌های مرور انتخاب‌شده در «مرورها»، Today و ویجت دیده شود و با بازکردن آن همان نقشه/شاخهٔ تعیین‌شده نمایش داده شود. ثبت انجام مطالعه باید موعد بعدی را طبق روش مرور به‌روزرسانی کند؛ نقشه یا کارت نباید با تسک عادی ادغام یا دادهٔ اصلی آن کپی/بازنویسی شود.
- آزمون end-to-end این دو جریان باید ثابت کند که تسک آموزشی روزانه به آیتم‌های واقعاً سررسیدشده محدود است، انتخاب پوشه/زیرفولدر/موضوع درست فیلتر می‌کند، نقشهٔ مشخص‌شده باز می‌شود، تیک/ثبت جلسه موعد بعدی را پایدار جابه‌جا می‌کند و همان نتیجه در وب، Today و ویجت همگام می‌ماند.

### نیازمندی تازهٔ Google Drive و فایل‌های چندرسانه‌ای درس

- **وضعیت کد در بررسی ۲۵ سپتامبر:** اتصال Drive/Google Picker یا Drive API پیدا نشد؛ `googleProvider` و `googleSignIn.ts` فقط برای ورود Firebase هستند و scopeای برای Drive درخواست نمی‌کنند. آپلود تصویر/ویدئو در `RichEditor` و `TaskAttachments` از Firebase Storage استفاده می‌کند؛ آپلود فایلِ خودِ `KnowledgeDocumentEditorModal` فقط HTML/HTM را می‌پذیرد. بنابراین ورود با حساب Google به معنی اتصال Drive نیست و در حال حاضر مسیر مستقیم انتخاب/آپلود رسانه از Drive برای درس تأیید نشد.
- ابتدا در کل برنامه، کد و تنظیمات بررسی شود آیا فقط Google sign-in وجود دارد یا اتصال واقعی Google Drive، انتخاب/آپلود رسانه و ذخیرهٔ ارتباط فایل با درس هم پیاده‌سازی شده است؛ این‌ها قابلیت‌های جدا هستند و نباید یکی به‌جای دیگری گزارش شود.
- اگر Drive واقعی موجود نیست، امکان اتصال امن حساب و افزودن تصویر/ویدئو به درس بررسی و سپس طراحی/پیاده‌سازی شود: انتخاب یا آپلود فایل از Drive، اتصال پایدار به سند/بخش درسی، پیش‌نمایش تصویر و پخش ویدئو، نمایش نام/نوع/اندازه، وضعیت بارگذاری و خطای قابل‌بازیابی. حذف پیوست از درس باید مشخص کند آیا فقط رابطه حذف می‌شود یا فایل Drive نیز، و هیچ فایل کاربر بدون تأیید صریح حذف نشود.
- OAuth باید با حداقل scope لازم و رضایت روشن کاربر طراحی شود؛ توکن یا secret در localStorage/کد سمت کاربر ذخیره نشود. لغو دسترسی، خروج/تعویض حساب، دسترسی‌نداشتن یا حذف‌شدن فایل، محدودیت اندازه/نوع، قطع شبکه و بارگذاری ناموفق باید حالت امن و قابل‌فهم داشته باشند. scope و سازگاری API پیش از پیاده‌سازی از مستندات رسمی روز Google تأیید شود.
- پذیرش: رسانه فقط به سند/بخش انتخاب‌شده متصل شود و بعد از refresh/ورود مجدد نیز درست بازیابی شود؛ لینک خصوصی بدون مجوز افشا نشود؛ preview و playback در وب موبایل/عریض کار کنند؛ شکست آپلود موفقیت کاذب ندهد. آزمون‌ها از حساب و فایل ساختگی استفاده کنند و به Drive زنده کاربر ننویسند.

### اصل تازهٔ زیبایی‌شناسی، ساختار و خوانایی همهٔ متن‌ها

- خوانایی و ارائهٔ جذابِ متن، اصل کیفیت محصول است و به Knowledge محدود نیست: تمام متن‌های آموزشی/نمایشی برنامه از نظر تیترگذاری، دسته‌بندی، پاراگراف‌بندی، فهرست‌ها و شماره‌گذاری، فاصله‌گذاری، یکپارچگی فارسی/انگلیسی و نمایش RTL/LTR بررسی شوند. شماره‌ها و موارد فهرست نباید به جملهٔ قبل بچسبند یا به شکل دیوار متنیِ پشت‌سرهم دیده شوند.
- راهنمای پایدار مشارکت‌های بعدی در `docs/KNOWLEDGE_CONTENT_STYLE_GUIDE.fa.md` ثبت شده و `AGENTS.md` رعایت آن را برای ساخت/ویرایش فایل‌های بعدی الزامی می‌کند. هر محتوای جدید پیش از تحویل در اندازهٔ موبایل، Foldable و دسکتاپ بازبینی شود؛ رنگ به‌تنهایی حامل معنا نباشد.
- **پایهٔ ممیزی محلی:** اسکن خواندنی `PHARMACY_SEED_DOCUMENTS` در `pharmacySeedData.ts`، ۴۳۲ سند دارای HTML یافت؛ در ۲۴۷ سند فهرست معنایی و در ۲۲ بلوک، شماره‌گذاری متوالی داخل یک متن عادی پیدا شد. Seed قدیمیِ ۹۷ سند نیز اسکن شد و چنین بلوکی نداشت. این آمار فقط seed محلی را پوشش می‌دهد، نه همهٔ اسناد Firestore کاربر یا تمام نوشته‌های UI؛ همهٔ منابع محتوای محلی و نمایشی (seed، HTML/Markdown/JSON، نوشته‌های UI و نمونه‌های واقعاً بارشده) باید در مرحلهٔ ممیزی فهرست شوند. یافته‌ها به ساختاری/نمایشی، دوزبانگی و RTL/LTR، و صحت علمی/منبع دسته‌بندی شوند؛ مورد مشاهده‌شده از ریسک استاتیک جدا بماند.
- **بهبود محلی آغازشده:** sanitizer نمایشی Knowledge اکنون پس از sanitization، فقط در بلوک متنیِ ساده و وقتی دست‌کم دو شمارهٔ متوالیِ روشن (ASCII/فارسی/عربی) تشخیص دهد، موارد را برای نمایش به `<ol>/<li>` تبدیل می‌کند. منبع/HTML ذخیره‌شده تغییر نمی‌کند؛ الگوهای غیرمتوالی، اعشاری و HTML دارای ساختار موجود دست‌نخورده می‌مانند. این تغییر باید با تست روی سند SafeScript، هر دو جهت متن، و رگرسیون لینک/کال‌اوت/تعامل تأیید شود؛ تبدیل خودکار جایگزین بازبینی همهٔ اسناد نیست.
- اصلاح فاصله، تیتر و ساختار فهرست از بازنویسی محتوا جدا باشد. شناسه‌ها، لینک‌ها، HTML معنایی و متن اصلی حفظ شوند؛ اصلاح خودکار فقط برای تغییرات قالبیِ قابل‌اثبات و قابل‌بازگشت مجاز است. هر بازنویسی یا تغییر ادعای بالینی/دارویی نیازمند منبع روز، حوزهٔ قضایی و بازبینی صلاحیت‌دار است و تولید AI به‌تنهایی تأیید علمی نیست.
- معیار پذیرش: نمونه‌های فارسی، انگلیسی و دوزبانه در عرض موبایل/پنل، Foldable باز و بسته، و دسکتاپ بدون فشردگی، بریدگی، ترتیب وارونهٔ عدد/نشانه، یا اسکرول افقی ناخواسته خوانده شوند؛ عنوان و سلسله‌مراتب قابل اسکن باشد؛ مراحل شماره‌دار واقعاً جدا باشند؛ رنگ با متن/آیکون/ساختار پشتیبانی شود؛ و مقایسهٔ پیش/پس ثابت کند که اصلاح نمایشی معنای علمی را عوض نکرده است.

### دروازهٔ ممیزی نهایی Knowledge، Review و راحتی مطالعه

- پس از اتمام مراحل انتخاب‌شده، مسیر واقعی Knowledge → لینک سند/دارو/بیماری → کارت/Leitner یا Mind Map → ثبت مرور → بازگشت به منبع → Today/تسک/ویجت به‌صورت یکپارچه و با آزمون رفت‌وبرگشت، ذخیره و همگام‌سازی بررسی شود؛ اشکال هر مرز میان ماژول‌ها باید جداگانه ثبت و رفع شود.
- کل جریان مطالعه در پنجرهٔ دسکتاپ ویندوز، حالت باریک/تقسیم‌صفحه، Foldable در حالت بسته و باز، و موبایل سنجیده شود: متن و کنترل‌ها نباید بریده یا پنهان شوند، مسیر بازگشت و جای مطالعه حفظ شود، و تراکم اطلاعات/حرکت/رنگ/اندازهٔ متن باعث خستگی چشم یا سردرگمی نشود. ادعای «مناسب همهٔ دستگاه‌ها» فقط پس از آزمون واقعی هر اندازه پذیرفته است.
- **بررسی معماری/کامپوننت‌بندی:** در پایان، مرز مسئولیت و وابستگی‌های `KnowledgeBaseView`، Reader/Editor، `ReviewView`، Leitner و Mind Map بررسی شود. کامپوننت‌بندی مجدد فقط وقتی انجام شود که مسئولیت‌های چندگانه، وابستگی‌پذیری ضعیف یا دشواری تست/نگهداری با شواهد روشن وجود داشته باشد؛ تغییر معماری باید مرحله‌ای، با حفظ قرارداد داده و تست رگرسیون باشد و صرفاً برای کوچک‌کردن فایل انجام نشود.
- تست نهایی باید با دادهٔ آزمایشی و بدون تغییر دادهٔ زندهٔ کاربر باشد و شامل دسترسی‌پذیری صفحه‌کلید/صفحه‌خوان، حالات loading/empty/error/offline، ذخیرهٔ پایدار، refresh، رفت‌وبرگشت، typecheck، build و آزمون UI باشد. APK اندروید ساخته نشود.
- **تحویل GitHub:** کاربر در ۲۵ سپتامبر صریحاً اجازه داده تغییرات بررسی‌شده چندین‌بار روی `main` منتشر شوند؛ push این پروژه به `main` سایت production را deploy می‌کند. این مجوز شامل تغییر کد وبِ کامل‌تست‌شده است، نه نوشتن/حذف محتوای زندهٔ Firestore یا انتشار APK. پیش از هر push وضعیت remote، diff، fast-forward بودن و آزمون‌های مناسب بررسی شود؛ هرگز دادهٔ واقعی کاربر به‌عنوان تست تغییر نکند.

### نتیجهٔ دور اجرایی ۲۵ سپتامبر

- Reader اکنون یک حالت مطالعه دارد؛ کنترل زبان فشرده بین English / فارسی / دوزبانه می‌چرخد و English پیش‌فرض سندهایی است که زبان ترجیحی ثبت‌شده ندارند. دکمه‌های AI، آموزش تعاملی و برنامه‌ریزی مطالعه آیکونی و دارای نام دسترس‌پذیر شده‌اند؛ Original HTML، کپی محتوا و بازکردن در تب بیرونی از این Reader برداشته شدند.
- اندازهٔ قلم به متغیر CSS متصل شد تا متن محتوای آموزشی و اندازه‌های متداول Tailwind در HTML سند واقعاً بزرگ/کوچک شوند. تست مؤلفه تغییر مقدار قلم و چرخهٔ زبان را پوشش می‌دهد.
- پس از پاک‌سازی HTML، پاراگراف‌های متن ساده‌ای که شماره‌گذاری پیوسته و بی‌ابهام دارند در زمان نمایش به فهرست معنایی تبدیل می‌شوند؛ متن اصلی Firestore/seed نوشته یا بازنویسی نمی‌شود و شماره‌های ناپیوسته/اعشاری دست‌نخورده می‌مانند. اسکن seed ثابت: ۴۳۲ سند HTML، ۲۴۷ سند دارای فهرست معنایی و ۲۲ متن دارای شماره‌گذاری پیوسته درون‌پاراگرافی؛ داده‌های زنده و همهٔ رشته‌های UI با این آمار پوشش داده نشده‌اند.
- تأیید محلی: `npm run typecheck` و `npm run build` موفق؛ Vitest با ۲ worker و timeout ده‌ثانیه‌ای: ۸۵ فایل/۴۷۸ تست موفق؛ `qa:layout` پیش‌تر ۱۰/۱۰ حالت را گذرانده؛ ESLint فایل‌های درگیر بدون خطا و با یک هشدار وابستگی hook موجود. build هنوز هشدار chunk بزرگ Pharmacy (حدود ۳٫۶ MB خام) و چند import ایستا/پویا دارد.
- **تأیید پس از انتشار:** commit `cdfab72` روی GitHub `main` تأیید شد و پس از refresh، سایت زنده نوار ابزار جدید Reader و فهرست معنایی شماره‌گذاری ۱ تا ۴ در سند SafeScript را نشان داد؛ کنترل‌های Original HTML / Copy / External نیز در DOM نبودند. عرض سند و viewport هر دو ۷۵۸px بود و اسکرول افقی صفحه دیده نشد. دسترسی API فهرست deployment در Vercel با 403 رد شد، بنابراین شناسه/وضعیت داخلی build در دسترس نبود؛ تأیید از خود سایت و نشانگرهای همین نسخه انجام شد. این بررسی read-only بود؛ سند زنده، دادهٔ کاربر، import Pharmacy و APK تغییر نکردند. نمایش در ابعاد و دستگاه‌های دیگر همچنان نیازمند آزمون است.

### ادامهٔ ممیزی Reader در ۲۵ سپتامبر

- آزمون read-only روی production مسیر بازکردن یک سند پیشنهادی و بازگشت داخلی به سند قبلی را گذراند؛ URL و عنوان سند در هر دو حالت درست تغییر کردند. هیچ محتوا یا دادهٔ Firestore تغییر نکرد.
- همان مشاهده یک ایراد ارائه را نشان داد: اسناد صرفاً هم‌پوشه‌ای با عنوانی شبیه «دانش بالینی مرتبط» نشان داده می‌شدند، هرچند ارتباط علمی/موضوعی نداشتند. عنوان به «پیشنهاد برای مطالعهٔ بیشتر» تغییر کرد؛ رتبه‌بندی، برچسب و علت تطبیق هر کارت نیز شفاف شده است. دور دوم مشخص کرد بعضی برچسب‌های پرتکرار مثل `Clinical Triage` به‌تنهایی ارتباط موضوعی دقیق نیستند؛ اکنون بسامد برچسب در مجموعه سنجیده و تطبیق برچسب خاص، دسته‌بندی عمومی، هم‌پوشانی واژه‌های عنوان و پوشه به‌طور جداگانه نمایش داده می‌شود. این فقط منطق نمایش/ترتیب است؛ هیچ متن یا دادهٔ دارویی عوض نمی‌شود.
- آزمون‌های هدف پس از دور دوم: ۱۶/۱۶؛ اجرای کامل Vitest: ۸۵ فایل/۴۸۲ تست؛ `npx tsc -p tsconfig.app.json --noEmit` موفق؛ ESLint فایل‌های درگیر ۰ خطا و یک هشدار قدیمی وابستگی hook؛ `npm run build` موفق. Build همچنان هشدارهای قبلی import ایستا/پویا و chunk بزرگ Pharmacy (حدود ۳٫۶ MB خام، ۴۱۴ KB فشرده) را دارد.
- پس از اعتبارسنجی، `git diff --check` پاک بود و `origin/main` با مبنای شاخه یکی بود؛ commit `71aefbf` push شد و نشانگر Vercel موفقیت deploy را تأیید کرد. در بازبینی زنده، پالایش دیگری در دلیل تطبیق عنوان لازم تشخیص داده شد و در بخش بعد آمده است.

### بازبینی زنده و پالایش دلیل تطبیق عنوان

- `71aefbf` روی `main` منتشر شد و نشانگر Vercel برای همان commit به `success` رسید. Reader زنده بار شد و برچسب‌های جدید را نشان داد؛ اما آزمون بصری خود یک مورد مرزی را آشکار کرد: واژه‌های پرتکرار عنوان‌های هم‌قالب («تریاژ»، «بالینی») به‌عنوان هم‌پوشانی اختصاصی سه سند نمایش داده شدند.
- `f275c5d` نیز push و deploy شد. پس از hard reload، پیشنهادها تغییر کردند و اصلاح توکن دقیق فعال بود؛ بااین‌حال واژه‌های عمومی پزشکی/قالبی مثل «داروی» و «تکرار» هنوز گاهی دلیل عنوانی می‌شدند. این مشاهده به‌دور سوم پالایش انجامید.
- پالایش نهایی، بسامد واژهٔ عنوان را می‌سنجد، واژه‌های عمومی فارسی/انگلیسی پزشکی و قالب‌های تکراری (از جمله red/alert/flag) را کنار می‌گذارد و به‌جای جست‌وجوی زیررشته، توکن‌های دقیق عنوان را مقایسه می‌کند. تست‌های رگرسیون برای سرعنوان مشترک، واژه‌های عمومی پزشکی و هشدارهای عمومی، و مثال substring افزوده شده‌اند. این فقط suggestion UI را دقیق می‌کند و متن/دادهٔ Firestore را تغییر نمی‌دهد.
- روی commit `690f1ca` وضعیت Vercel `success` شد؛ `vercel inspect` دامنهٔ اصلی را به deployment آمادهٔ production با سه alias شامل `arshiam.vercel.app` نشان داد. پس از hard reload، Reader نسخهٔ به‌روز را اجرا کرد؛ اشتراک تک‌واژه‌ای `red` به‌عنوان مورد مرزی ثبت و اکنون رفع شده است.
- پس از افزودن واژه‌های عمومی medical alert، suiteهای هدف ۲۰/۲۰، full Vitest برابر ۸۵ فایل/۴۸۶ تست، `npx tsc -p tsconfig.app.json --noEmit` و `npm run build` موفق شدند؛ ESLint فایل‌های درگیر ۰ خطا و یک هشدار قدیمی وابستگی hook. Build همچنان چند هشدار import ایستا/پویا و chunk Pharmacy حدود ۳٫۶ MB خام/۴۱۴ KB gzip را گزارش می‌کند.
- commit `96adce6` روی `main` push شد؛ GitHub/Vercel وضعیت `success` داد و CLI دامنهٔ اصلی را به deployment production آماده متصل نشان داد. پس از hard reload، Reader زنده نشان داد تطبیق‌های عمومی `red` حذف شده‌اند و سه پیشنهاد اکنون بر اساس واژهٔ دقیقِ jurisdiction یعنی `NSW` برچسب می‌خورند. آزمون فقط خواندنی بود؛ هیچ متن یا دادهٔ Firestore، import، APK یا حساب کاربر تغییر نکرد.

## گراف بالینی منتقل‌شده از Pharmacy

- اسکریپت `scripts/generatePharmacyClinicalGraph.mjs` رجیستری را از Pharmacy با شناسهٔ commit ثابت `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` می‌خواند و `src/lib/pharmacyClinicalGraph.generated.ts` را می‌سازد؛ نگاشت فقط بر پایهٔ شناسه‌های قطعی seed است و از تطبیق حدسی عنوان استفاده نمی‌کند.
- گراف منبع شامل ۴۴۲ گره و ۵۴۶ رابطه است: بیماری ۵۸، فرآورده ۱۲۱، مادهٔ مؤثره ۱۲۰، سناریو ۳۲، مفهوم بالینی ۳۵، CYP ۶ و مکانیسم ۷۰.
- ۳۲۲ گره به سند موجود وصل می‌شوند؛ ۱۲۰ گرهٔ مادهٔ مؤثره عمداً سند ساختگی نمی‌گیرند و در UI غیرقابل‌کلیک می‌مانند. هر ۵۴۶ رابطه و شناسهٔ دو سر آن حفظ شده؛ ۳۵۴ رابطه دو سند قابل‌پیوند دارند و در ۱۹۲ رابطه دست‌کم یک سر، گرهٔ بدون سند مستقل است.
- رجیستری ۱۲۷ رابطه را `verified` و ۴۱۹ را `suggested` برچسب زده است. فقط ۶ مورد از ۱۲۷ مورد اول دو سند قابل‌پیوند دارند؛ UI صریحاً می‌گوید این برچسب‌ها از کد منبع آمده‌اند، نه بازبینی بالینی مستقل. منشأ و دلیل داخلی رابطه‌ها نیز نمایش داده می‌شود.
- شمارش متن‌محور `data-doc-link` تعداد ۵۳۴ پیوند یکتا یافت؛ فقط ۴۸ زوج سند با یال‌های رجیستری در یکی از دو جهت منطبق بودند. این اختلاف به‌تنهایی اثبات خطا نیست، چون HTML و رجیستری دامنه/نوع متفاوت دارند؛ اما نشان می‌دهد audit معنایی یال‌ها هنوز لازم است.
- تست‌های گراف و کارت رابطه ۱۵ مورد موفق شدند؛ در آخرین دور، کل Vitest شامل ۸۵ فایل و ۴۶۵ تست موفق شد و `npm run lint` با صفر خطا و ۶۰ هشدار موجود، typecheck و production build نیز موفق بودند. build همچنان هشدارهای قبلیِ importهای static/dynamic و chunk بزرگ Pharmacy را می‌دهد؛ chunk شبکهٔ رابطه ۲۵۳٫۹۴KB (فشرده ۴۰٫۶۰KB) و خروجی فعلی ۱۵۷ فایل precache با حدود ۶٫۱۷MB است. کد وب منتشر شده، اما APK ساخته و Firestore زنده نوشته نشده است.

## بررسی بصری Chrome (نسخهٔ production فعلی)

- مسیر بازشده `/app/knowledge`، اندازهٔ viewport پنل Chrome: `758×583`. صفحه بدون overflow افقی بود و در لاگ‌های همان tab خطا/هشداری ثبت نشد.
- در این عرض، مسیر/عنوان بالای سند در نوار بالایی ناقص/بریده دیده می‌شود؛ برای حکم قطعی به بازبینی در پنجرهٔ عریض Chrome هم نیاز است.
- سند نمایشی CYP1A2 خود هشدار می‌دهد ترجمهٔ فارسی ناقص است؛ بدنهٔ فارسی/انگلیسی هنوز بخش‌هایی از متن انگلیسی را دارد. این وضعیت محتوای production فعلی است، نه نتیجهٔ تغییرات محلی تا وقتی import جداگانه در Firestore مجاز و اجرا شود.

## وضعیت پس از ادامهٔ کار (2026-09-25)

- مسیر مستقل `/app/interactive-study` افزوده شد: انتخاب و جست‌وجوی درس، زبان مطالعه با پیش‌فرض انگلیسی، ساخت تمرین تعاملی و پایان/تعویض جلسه. HTML هنگام نمایش sanitize می‌شود و کنترل‌های تعاملی با listenerهای محدود به همان جلسه فعال می‌شوند.
- در مرحلهٔ اولیه جلسه فقط state موقت صفحه بود؛ این محدودیت در اصلاح محلی بعدی که پایین‌تر ثبت شده برطرف شد. این قابلیت هنوز شبیه‌سازهای تصمیم‌گیری بالینی، OTC triage یا dispensing موجود در Pharmacy را بازتولید نمی‌کند؛ parity محتوایی/بالینی همچنان باقی است و هیچ محتوای بالینی تأییدنشده برای استفادهٔ واقعی عرضه نمی‌شود.
- Vitest کامل در زمان این snapshot: 86 فایل و 491 تست موفق؛ `npx tsc -p tsconfig.app.json --noEmit` موفق؛ `npm run build` موفق؛ lint با صفر خطا و 59 هشدار پیشین. Build هشدارهای قبلی importهای هم‌زمان پویا/ایستا و chunkهای بزرگ Pharmacy را نشان می‌دهد. نتیجهٔ تازه‌تر پایین‌تر ثبت شده است.
- بررسی Chromium محلی به صفحهٔ ورود هدایت شد؛ برای جلوگیری از استفاده/درخواست اطلاعات ورود، گردش کامل جلسه در مرورگر end-to-end انجام نشد. تعامل‌ها در تست‌های کامپوننت پوشش دارند؛ بررسی بصری نسخهٔ production پس از deploy هنوز انجام نشده است.
- هیچ دادهٔ زندهٔ Firestore یا APK تغییر/ساخته نشد.

## اصلاح محلی بعدی — مرز هویت در برنامه‌ریزی مطالعه (2026-09-25)

- در `StudyTaskScheduleModal`، نبودن شناسهٔ حساب می‌توانست به‌جای توقف، تسک را با `anonymous-study-user` بسازد. علاوه بر آن، `createStudyTask` در صورت پاسخ `false` از ذخیره‌ساز، task ساخته‌نشده را در نتیجه برمی‌گرداند.
- فرم اکنون بدون کاربر واردشده پیش از نوشتن متوقف می‌شود و پیام ورود نشان می‌دهد؛ سرویس نیز شناسهٔ کاربر/هدف خالی را رد می‌کند و فقط پس از ذخیرهٔ موفق، task را برمی‌گرداند. برای پاسخ ناموفق، هیچ رابطهٔ task-document ساخته نمی‌شود.
- تست رگرسیون UI و سرویس: ۷/۷؛ اجرای کامل Vitest: ۸۷ فایل/۴۹۵ تست؛ `npx tsc -p tsconfig.app.json --noEmit` و ESLint فایل‌های تغییرکرده موفق؛ `npm run build` موفق. آزمون چیدمان دسکتاپ پیش‌تر ۱۰/۱۰ حالت را گذرانده؛ این اصلاح ظاهر را عوض نمی‌کند. Build همچنان هشدارهای شناخته‌شدهٔ chunk بزرگ و importهای ایستا/پویا دارد.
- تغییرهای این مرحله فقط محلی و بازبینی‌نشده برای انتشارند؛ به GitHub `main`، Firestore زنده یا APK نرفته‌اند.

## یافتهٔ بررسی منبع بالینی Pharmacy — نیازمند بازبینی داروساز (2026-09-25)

- در منبع Pharmacy، `data/scenarios/clinicalScenarios.ts:1101`، بازخورد سناریوی آفتاب‌سوختگی می‌گوید تاول‌های درجه‌دو با وسعت بیش از ۲۰٪ سطح بدن و علائم سیستمیک نشان‌دهندهٔ `sun poisoning` هستند و به احیای مایعات و مراقبت سوختگی بیمارستانی نیاز دارند؛ نامهٔ ارجاع همان سناریو نیز همین آستانه را تکرار می‌کند.
- مدل Scenario این سناریو ارجاع به فایل Pharmacy را دارد، اما برای این ادعای بالینی منبع راهنمای بیرونی، حوزهٔ قضایی، تاریخ دسترسی/بازبینی یا نام بازبین ندارد. بنابراین منشأ GitHub فقط محل کد را نشان می‌دهد و شاهد علمی/بالینی نیست.
- این مورد را «ریسک ایمنی/ردیابی منبع و احتمال خلط آستانه‌ها» ثبت می‌کنیم، نه حکم قطعیِ نادرستی: راهنمای جاری ACI در NSW برای سوختگی‌های dermal معیارهای ارجاع مبتنی بر وسعت و محل دارد؛ پروتکل جداگانهٔ ACI برای کارکنان درمانی دربارهٔ احیای مایعات است و نباید آستانهٔ آن با معیار ارجاع یا توصیهٔ OTC یکی گرفته شود. Healthdirect نیز سوختگی/تاول گسترده و علائم شدید را دلیلی برای ارزیابی پزشکی می‌داند. زمینه، سن، عمق سوختگی و حدود صلاحیت حرفه‌ای باید توسط داروساز/پزشک واجد صلاحیت بازبینی شود.
- منابع رسمی برای بازبینی: [NSW ACI ED burns flow chart](https://aci.health.nsw.gov.au/networks/burn-injury/resources/patient-management/ed-flow-chart)، [NSW ACI adult ECAT burns protocol](https://aci.health.nsw.gov.au/ecat/adult/burns)، و [Healthdirect sunburn](https://www.healthdirect.gov.au/sunburn).
- اقدام ایمن تا بازبینی: این متن را به‌عنوان محتوای بالینی تأییدشده به ARSHNAZ منتقل/منتشر نکنید و پاسخ خودکار سناریو را مرجع تصمیم‌گیری OTC تلقی نکنید. پیش از parity، برای همهٔ سناریوها منبع اولیه، jurisdiction، reviewed/accessed date و reviewer را ثبت کنید؛ سناریوی فاقد این فراداده باید هشدار عدم بازبینی نشان دهد و پاسخ قطعیِ «صحیح/غلط» بالینی ندهد.

## آزمون رگرسیون مسیر Thought Records (2026-09-25)

- مسیر مستقیم `/app/thoughts` در نسخهٔ فعلی به `ThoughtRecordsView` می‌رسد و با کاربر ساختگی و تاریخچهٔ خالی بدون crash رندر می‌شود؛ برای جلوگیری از بازگشت خطای قبلی، تست `src/pages/ThoughtRecordsView.test.tsx` اضافه شد.
- اعتبارسنجی: اجرای کامل Vitest با ۲ worker برابر ۹۲ فایل/۵۴۱ تست موفق؛ `npx tsc -p tsconfig.app.json --noEmit` و ESLint دو فایل درگیر موفق. دادهٔ Firestore زنده تغییر نکرد و APK ساخته نشد.

## حفظ دامنهٔ درس در تسک مرور Leitner (2026-09-25)

- هنگام زمان‌بندی تسک Leitner، کاربر اکنون می‌تواند همهٔ کارت‌ها یا یک درس مشخص را انتخاب کند. شناسهٔ درس به `source_id` تسک می‌رود؛ بازکردن همان تسک با `studyDocId` فقط کارت‌های سررسیدشدهٔ همان درس را برای جلسه می‌آورد. مسیر «همهٔ کارت‌ها» بدون تغییر مانده است.
- تست مسیر انتخاب درس → ذخیرهٔ هدف → ناوبری → فیلتر کارت سررسیدشده اضافه شد. اعتبارسنجی این مرحله: تست هدف ۱۶/۱۶، Vitest کامل ۹۳ فایل/۵۴۴ تست، typecheck، ESLint و build موفق؛ آزمون چیدمان موجود Today نیز ۱۰/۱۰ حالت را گذراند. Build فقط هشدارهای قبلی chunk بزرگ Pharmacy و importهای ایستا/پویا را دارد.
- این کار **نیازمندی Reviews را کامل نمی‌کند**: مرورها هنوز رکوردهای task هستند؛ انتخاب فولدر/زیرفولدر یا چند موضوع، تولید خودکار آیتم‌های موعددار، نمایش مستقل Review در Today/ویجت و جابه‌جایی پایدار موعد با تکمیل جلسه هنوز انجام نشده‌اند. آزمون چیدمان ۱۰حالت نیز مربوط به Today است، نه Reader/Leitner در همهٔ ابعاد.
- هیچ دادهٔ زندهٔ Firestore تغییر نکرد و APK ساخته نشد.

## نیاز تازهٔ طراحی Mind Map و مرجع Pharmacy (2026-09-25)

- درخواست حین بازبینی زنده: عنوان فصل/زیرفصل باید به‌جای بریده‌شدن تا حد ممکن کامل و چندخطی دیده شود؛ کنترل‌های کم‌استفاده از فضای عنوان بیرون بروند و در منوی سه‌نقطهٔ جمع‌وجور قرار گیرند. کاربر می‌خواهد رنگ عنوان/گره و شکل سلول هم قابل تنظیم باشد. گام نخستِ محلی در نمای Mind Map انجام شده: حالت Outliner تو‌در‌تو، عنوان چندخطی، ابعاد پویای گره و منوی کنش فشرده اضافه شده است؛ تنظیم رنگ/شکل و آزمون بصری همهٔ عرض‌ها هنوز باقی است.
- در مخزن Pharmacy با commit `5b4f7d2` وجود پنج حالت `interactive_canvas`, `radial_circle`, `vertical_tree`, `outliner_tree`, `matrix_grid` در `LeitnerMindMapPanel`، `MindMapCanvas` و `MindMapSettingsModal` تأیید شد؛ `BranchStudyRunnerModal` نیز وجود دارد. از این‌ها به‌عنوان الگوی انتخاب‌پذیری و مطالعهٔ شاخه استفاده شود، نه الزام به کپی عین UI/معماری.
- ادعاهای دیگر متن مرجع—از جمله هفت perspective، چهار سبک خط، هفت سطح نود، همهٔ انواع کارت بالینی، backup/restore، پروفایل‌های FSRS یا export—تا وقتی در کد منبع Pharmacy جداگانه پیدا و رفتاری آزموده نشده‌اند، «قابلیت تأییدشده» محسوب نشوند. ابتدا مقایسهٔ واقعی و سپس انتخاب گزینه‌های مفید برای ARSHNAZ.
- معیار طراحی پیشنهادی: عنوان و اقدام اصلی در تمام عرض‌ها مقدم بمانند؛ منو نام دسترس‌پذیر داشته باشد؛ انتخاب رنگ/شکل با کنتراست کافی و ترجیح ذخیرهٔ per-user/per-map طراحی و آزموده شود؛ رنگ به‌تنهایی معنا را منتقل نکند و تنظیمات موجود/راهنمای ARSHNAZ حفظ شوند.

## نیاز تازهٔ Outliner قدرتمند برای Mind Map و Leitner (2026-09-25)

- درخواست: نمای Outliner در ARSHNAZ به‌طور محسوس از نمونهٔ Pharmacy کامل‌تر باشد و در هر دو فضای Mind Map و Leitner کار کند. نخستین برش Mind Map اکنون فقط به‌صورت تغییر محلی پیاده شده و تست ساختگی دارد؛ هنوز در GitHub یا سایت اصلی منتشر نشده است. Outliner لایتنر و بیشتر قابلیت‌های پیشرفتهٔ زیر اجرا نشده‌اند.
- برش فعلی: جابه‌جایی Canvas/Outline، درخت والد/فرزند با حفظ وضعیت بازشدن، عنوان کاملِ چندخطی، ردیف‌های lazy-render با `content-visibility`, فلش مستقلِ گسترش کارت‌های درس، و منوی کنش فشرده شامل Reader/تمرکز/زمان‌بندی. زمان‌بندی کارت به سند منبع نگاشت می‌شود و اگر سند منبع پیدا نشود کنش پنهان/رد می‌شود؛ به‌جای هدف‌گرفتن اشتباه کل دانش.
- اعتبارسنجی این مرحله: Vitest کامل ۹۶ فایل و ۵۶۹ تست موفق؛ typecheck، build و ESLint فایل‌های Mind Map موفق؛ `git diff --check` بدون خطا. build همچنان هشدار قبلی chunk بزرگ Pharmacy و importهای static/dynamic را می‌دهد. نمای مرورگر در اندازه‌های واقعی موبایل/تاشو/دسکتاپ هنوز بصری آزموده نشده است.
- Outliner نقشهٔ ذهنی باید ساختار والد/فرزند را با تورفتگی و راهنمای سطح واضح حفظ کند؛ عنوان‌های بلند را چندخطی نشان دهد؛ گسترش/جمع‌کردن سریع هر شاخه و همهٔ شاخه‌ها، جست‌وجو با بازشدن خودکار مسیر نتیجه، breadcrumb والد، شمارش زیرشاخه/سند/کارت/موعد، و منوی کنش جمع‌وجور داشته باشد. میانبرهای صفحه‌کلید و کار با لمس، انتخاب چند نود، تغییر ترتیب/والد با حفظ شناسه و رابطه، و تنظیم رنگ/شکل قابل دسترس نیز ارزیابی شوند.
- Outliner لایتنر باید درخت پوشه/درس/دستهٔ مرور را کنار ساختار جعبه و وضعیت موعد قابل پیمایش کند؛ جست‌وجو و فیلتر بر اساس درس، سررسید، جعبه، دشواری/لغزش؛ انتخاب و مرور یک شاخه یا مجموعه؛ و کنش‌های گروهی امن مثل برچسب/ستاره/آرشیو/تعلیق و ویرایش گروهیِ کنترل‌شده فراهم کند. جابه‌جایی دستی جعبه یا موعد نباید بی‌توضیح الگوریتم FSRS/SM-2 را دور بزند.
- تنظیمات نمایش مشترک پیشنهادی: تراکم ردیف و فاصله، اندازه/وزن عنوان، نمایش/پنهان‌سازی نشانگرها، مرتب‌سازی، رنگ و شکل نود/ردیف با کنتراست و دسترس‌پذیری؛ حفظ انتخاب و مسیر بازشده هنگام جابه‌جایی نما و پس از بازگشت.
- معیار پذیرش: عنوان هیچ‌گاه زیر آیکن/کنش پنهان نشود؛ تعداد زیاد نود/کارت قابل استفاده بماند؛ هر کنش واضح و قابل بازگشت/تأیید باشد؛ RTL/LTR و عرض موبایل، تاشو و دسکتاپ آزموده شود؛ و همهٔ مرتب‌سازی/انتخاب/ویرایش‌ها شناسه، والد/فرزند، موعد الگوریتمی و مالکیت داده را حفظ کنند.

## ارزیابی کاربردی Interactive Study Studio (2026-09-25)

### اقدام UX اجراشده در checkout محلی (2026-09-25)

- انتخاب پیش‌فرض درس حذف شد؛ فیلتر پوشه با شمول زیرپوشه‌ها، گزینهٔ «بدون پوشه»، جست‌وجو و شمارندهٔ نتایج اضافه شد. اگر فیلتر/جست‌وجو درس فعال را پنهان کند، انتخاب و پیش‌نویس فعال پاک می‌شوند.
- فهرست در موبایل محدود و پیمایش‌پذیر است؛ از عرض ۷۲۰px چیدمان دوپنلی با ارتفاع کنترل‌شده دارد. در Chrome محلی در عرض حدود ۷۴۸px بررسی شد؛ پایگاه محلی درس نداشت، پس نمایش واقعی فهرست/جلسه با دادهٔ production دست‌کاری نشد.
- پیش‌نویس Interactive Study اکنون در کد محلی ذخیره/بازیابی می‌شود: Firestore با cache و صف آفلاین، وضعیت ذخیرهٔ قابل‌مشاهده، ادامهٔ پیش‌نویس همان درس/زبان، و محافظ خروج هنگام ذخیره‌نشده‌بودن. این مسیر در برابر دادهٔ زنده end-to-end آزموده نشده و به تسک/Leitner وصل نیست؛ امتیاز/تاریخچه و شبیه‌سازی کامل OTC/FRED نیز هنوز انجام نشده‌اند. هیچ تولید AI، نوشتن دادهٔ production، انتشار یا push انجام نشد.
- آزمون‌های صفحه با دادهٔ ساختگی: ۴/۴ در snapshot قبلی؛ نتیجهٔ تازه‌ترِ مجموعهٔ کامل پس از مراحل بعدی ۹۶ فایل و ۵۶۹ تست موفق است (جزئیات Outliner در بخش پایین).
- مسیر جداگانهٔ «تسک مرور Leitner» به جلسهٔ موعددار وصل است: شناسهٔ تسک/درس از URL به مرور می‌رسد؛ پس از پایان جلسه، موعد بعدی از کارت‌های همان درس/دسته پیدا و فقط در صورت تطبیق مالکیت، نوع و هدفِ تسکِ فعال ذخیره می‌شود؛ حالت آفلاین از cache کاربر و صف همگام‌سازی استفاده می‌کند. این مسیر خودش جلسهٔ Interactive Study را ایجاد/ذخیره یا به Leitner تبدیل نمی‌کند؛ پیش‌نویس Interactive Study اکنون مسیر ذخیره/بازیابی مجزایی دارد. آزمون end-to-end روی دادهٔ واقعی انجام نشده است.

- بررسی زندهٔ `/app/interactive-study` و کد `InteractiveStudyView` نشان می‌دهد صفحه برای انتخاب یکی از درس‌های Knowledge Base، انتخاب زبان و ساخت تمرین AI در همان صفحه است. قالب‌های کدنویسی‌شده شامل فلش‌کارت، MCQ، تطبیق جفت، سناریوی مرحله‌ای، جای‌خالی، درخت تصمیم و بازی حافظه‌اند؛ این با یک شبیه‌ساز واقعیِ مشاوره OTC یا تمرین گردش‌کار Fred یکی نیست.
- صفحه ۳۷۵ درس نشان می‌داد و درس نخست (`CYP1A2`) از پیش انتخاب شده بود. کاربر باید از فهرست بلند درس، دکمهٔ ساخت جلسه و سپس قالب‌ها/تولید/شروع عبور کند؛ صفحه پیش از تولید نمونهٔ قابل‌اجرا یا راهنمای روشنِ مسیر ندارد. پیاده‌سازی متن هر درس را برای تولید به حداکثر ۳۵۰۰ نویسه کوتاه می‌کند؛ بنابراین ممکن است همهٔ درس در تمرین لحاظ نشود.
- محدودبودن جلسه به ماندن در همان صفحه، یافتهٔ نسخهٔ پیش از اصلاح ذخیره‌سازی بود و دیگر وضعیت کد محلی را توصیف نمی‌کند. در نسخهٔ محلی فعلی ذخیره/بازیابی پیش‌نویس و محافظ خروج افزوده شده؛ بااین‌حال امتیاز/تاریخچهٔ پیشرفت، مرور هدفمند، اتصال خودکار این جلسه به تسک/Leitner و شبیه‌ساز کامل OTC/FRED هنوز وجود ندارد. بنابراین اعتراض کاربر به مسیر آموزشی و کاربرد نامشخص هنوز معتبر است، اما به معنی اثبات خرابی همهٔ ویجت‌ها نیست.
- نیاز طراحی پیشنهادی: ورودی از حالت فهرست ۳۷۵تاییِ بی‌زمینه به یک استودیوی آموزشیِ قابل فهم تبدیل شود؛ کاربر نوع تمرین (مثلاً مشاورهٔ تعاملی بیمار، تریاژ، تمرین FRED، quiz/flashcards), موضوع، سطح و زبان را انتخاب کند؛ پیش از شروع هدف، مراحل و زمان تقریبی را ببیند؛ جلسه را ذخیره/ادامه دهد و نتیجه/نقاط نیازمند تمرین را مرور کند. تولید کارت یا ساخت task مرور باید اختیاری و با منشأ مشخص باشد، نه انتقال اجباری.
- برای محتوای پزشکی/دارویی، منبع مورد استفاده، وضعیت بازبینی، بخش دقیقِ منبعی که سؤال از آن ساخته شده و محدودیت‌های قطعیت باید همراه خروجی قابل مشاهده باشند. محتوای تولیدشدهٔ AI بدون بازبینی انسانی نباید با برچسب «تأییدشده» یا پاسخ قطعی بالینی ارائه شود.
- معیار پذیرش: هیچ درس به‌طور پنهانی پیش‌فرض نشود؛ فهرست با دسته/موضوع و جست‌وجوی قابل فهم محدود شود؛ مسیر انتخاب تا شروع کوتاه و دارای نمونه باشد؛ خروج از صفحه جلسه را بی‌هشدار از بین نبرد؛ پیشرفت فقط پس از ذخیرهٔ موفق «ثبت‌شده» تلقی شود؛ و RTL/LTR، موبایل، تاشو و دسکتاپ و محتوای فارسی/انگلیسی آزموده شوند. فیلتر/چیدمان و ذخیره/بازیابی اکنون در checkout محلی پیاده شده‌اند؛ مسیر کامل تولید/ادامه/اتصال و آزمون بصری چنداندازه‌ای هنوز باقی است.

### اصلاح بعدی: ذخیره و بازیابی امن جلسهٔ تعاملی (2026-09-25)

- جلسه در مجموعهٔ خصوصی کاربر `interactive_study_sessions` و خارج از سند درس/کارت Leitner ذخیره می‌شود؛ شناسهٔ پایدار سند و زبان حفظ می‌شوند. پایان جلسه فقط پس از تأیید ذخیره/صف پایدار آن را `completed` می‌کند.
- تعامل‌های خود ویجت با debounce کوتاه ذخیره می‌شوند؛ ذخیره‌ها ترتیبی‌اند و پیش از تعویض درس/زبان و پایان جلسه flush می‌شوند. هنگام خروج SPA نیز ذخیرهٔ پایانی آغاز می‌شود؛ خروج/بستن کامل مرورگر در حالت saving/failed هشدار native می‌دهد. این هشدار جلوی از‌دست‌رفتن هنگام تأیید اجباری خروج را تضمین نمی‌کند، پس باید وضعیت «ذخیره‌شده» یا «در صف همگام‌سازی» دیده شود. برای شکست، retry و هشدار روشن وجود دارد.
- آفلاین، محتوای جلسه فقط پس از ثبت موفق در outbox پایدار پذیرفته می‌شود؛ mutationها مالک شناسهٔ همان کاربر را حمل می‌کنند و در صف فقط برای همان حساب replay می‌شوند. جلسهٔ cache‌شده در حالت بدون شبکه مبنای بازیابی است و mutationهای همان مالک روی آن اعمال می‌شوند. cache کمکی اگر شکست بخورد، نتیجهٔ ذخیرهٔ تأییدشده را به شکست کاذب تبدیل نمی‌کند.
- HTML هنگام ساخت، بازیابی، ذخیره و نمایش sanitize می‌شود؛ حد ۷۰۰ KiB برای payload دارد و با serializing ریشهٔ واقعی widget از انباشته‌شدن wrapper در هر بار بازیابی جلوگیری می‌شود.
- قفل stale-write برای این collection نسخهٔ برنامه (`updated_at`) را با همان فیلد مقایسه می‌کند، نه زمان رسیدن آخرین sync (`updatedAt`)، تا timestamp رسیدِ دیرتر ذخیرهٔ تعامل تازه را بی‌جهت رد نکند؛ قاعدهٔ قبلی سایر collectionها بدون تغییر است.
- پوشش آزمایش شامل بازیابی بر اساس سند/زبان، جداسازی مالک، fallback آفلاین، عدم موفقیت کاذب در صورت شکست cloud و outbox، ذخیرهٔ حالت flip، تکمیل جلسه پس از ذخیره و retry شکست، حفاظت beforeunload، جلوگیری از wrapperهای تکراری و محافظت timestamp stale-write است. تست متمرکز ۳۸/۳۸، Vitest کامل ۹۴ فایل/۵۶۴ تست، typecheck و ESLint فایل‌های تغییرکرده موفق شدند.
- `npm run build` موفق شد؛ هشدارهای قبلی importهای static/dynamic و chunkهای بزرگ seed/Pharmacy باقی‌اند. گردش کامل در مرورگر با دادهٔ زنده یا Firebase Emulator، بازیابی بین دو دستگاه، اشتراک‌گذاری حساب و آزمون بصری production در این مرحله اجرا نشده است؛ هیچ دادهٔ Firestore زنده، GitHub `main`، deployment یا APK تغییر نکرد.
- مرز باقی‌مانده: تاریخچهٔ چندجلسه‌ای قابل‌مرور، امتیاز/تحلیل نتایج، اتصال اختیاری به تسک و شبیه‌سازهای واقعی OTC/FRED هنوز پیاده‌سازی نشده‌اند؛ جلسهٔ فعلی یک تجربهٔ ذخیره‌پذیر برای انواع ویجت موجود است و هم‌ارزی کامل Pharmacy محسوب نمی‌شود.

## برش محلی بعدی: نمای Outliner لایتنر (2026-09-25)

- در فهرست کارت‌ها حالت اختیاری فهرستی/درختی اضافه شد؛ حالت فهرستی قبلی پیش‌فرض می‌ماند. نمای درختی پوشه ← درس ← کارت را نشان می‌دهد، شاخه‌ها باز/بسته می‌شوند و متن کامل پرسش/پاسخ چندخطی است.
- کارت متصل به یک درس فقط یک‌بار زیر همان درس می‌آید؛ شناسهٔ واقعی کارت و درس حفظ می‌شوند. پیوند درس حذف‌شده به پوشهٔ معتبر fallback می‌شود و کارت بدون والد معتبر در «بدون پوشه یا درس» گم نمی‌شود. چرخهٔ احتمالی پوشه‌ها نیز با قطع قطعی یک یال، بدون حلقهٔ رندر و بدون پنهان‌کردن کارت مدیریت می‌شود.
- جست‌وجو و فیلتر جعبه حفظ شدند؛ فیلتر موعد از شناسه‌هایی استفاده می‌کند که سرویس مرور واقعاً موعددار برگردانده، نه محاسبهٔ تاریخ جداگانه. فیلتر کارت‌های لغزش‌خورده نیز اضافه شد. هیچ کنترل دستی برای تغییر موعد/جعبه افزوده نشد و ارزیابی پاسخ همچنان از مسیر الگوریتم زمان‌بندی موجود انجام می‌شود.
- هر پوشه/درس دکمهٔ مرور کارت‌های موعددار همان شاخه دارد؛ جلسه از شناسه‌های واقعی کارت ساخته می‌شود و در تسکِ مرورِ درس‌محور، فقط کارت‌های واجد شرایط همان درس قابل شروع‌اند. تکمیل مرور یک شاخه، تسک پیوندخورده را جلو نمی‌اندازد؛ تسک فقط پس از پایان صفِ کامل خودش طبق موعد الگوریتمی جابه‌جا می‌شود. شاخهٔ «بدون پوشه یا درس» نیز مرور گروهی کارت‌های سررسیدشده را دارد.
- فهرست درس‌ها و پوشه‌ها جدا از تازه‌سازی کارت‌ها بارگذاری می‌شود؛ پس از هر امتیاز کارت، ساختار دانش دوباره خوانده نمی‌شود. این کار تعداد درخواست‌های تکراری را در جلسهٔ مرور کم می‌کند.
- اعتبارسنجی محلی: تست متمرکز helper و رابط ۱۴/۱۴؛ کل Vitest برابر ۹۷ فایل/۵۷۵ تست؛ `npx tsc -p tsconfig.app.json --noEmit`، ESLint فایل‌های تغییرکرده، `git diff --check` و `npm run build` موفق‌اند. build همچنان هشدارهای importهای static/dynamic و chunk بزرگ دادهٔ Pharmacy (`pharmacySeedData` حدود ۳٫۶ MB قبل از gzip) را می‌دهد. مرورگرِ داده‌شده در زمینهٔ این نوبت شناسهٔ دیگری داشت و از Chrome پاسخ `Tab not found` گرفت؛ بنابراین این دور اعتبارسنجی بصری/تعاملی صفحه انجام نشد. تست کامپوننتِ شاخه با دادهٔ ساختگی انجام شده؛ دادهٔ Firestore زنده دست‌کاری نشد.
- این برش به GitHub، Firestore زنده، deployment یا APK نرفته است. مرور یک شاخه اکنون پیاده‌سازی شده؛ انتخاب چندگانه، کنش‌های گروهیِ امن، جابه‌جایی نودها، تنظیم رنگ/شکل/تراکم و آزمون بصری viewportهای موبایل/تاشو/دسکتاپ همچنان باقی‌اند.

## پایداری درخت Knowledge و بازبینی snapshot داروخانه (2026-09-25)

- ریسک کدیِ بازبینی‌شده: چرخهٔ `parent_id` در درخت اصلی Knowledge می‌توانست همهٔ پوشه‌های چرخه‌ای را از ریشه جدا کند؛ دو پیمایش برای بازکردن خودکار والدهای سند/پوشه نیز محافظ تکرار نداشتند. پوشه با والد حذف‌شده به ریشه می‌رود؛ چرخه با قطع قطعی رابطهٔ پوشه‌ای که شناسهٔ واژه‌نامه‌ای کوچک‌تری دارد به یک درخت قابل‌پیمایش تبدیل می‌شود، بدون تغییر IDها.
- سندی که `folder_id` تهی یا ناموجود دارد دیگر در سایدبار ناپدید نمی‌شود و در گروه «اسناد بدون فولدر یا با فولدر ناموجود» دیده می‌شود. رابطهٔ ذخیره‌شده را خودکار بازنویسی نمی‌کنیم؛ این نما فقط سند را قابل‌بازیابی می‌کند. بازکردن خودکار ancestorها با شناسهٔ دیده‌شده متوقف می‌شود.
- بررسی read-only منبع: `git ls-remote` برای `hamedharami-hub/pharmacy`، `HEAD` و `refs/heads/main` را روی `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` نشان داد؛ این با commit گراف بالینیِ تولیدشده برابر است. هر ۴۳۲ URL منبع در seed نیز به همان commit اشاره می‌کند. آزمون seed می‌پوشاند: ۳۴ پوشه، ۴۳۲ سند، لینک‌های داخلی به ID سند موجود، پوشهٔ موجود برای هر سند و هدف موجود برای کارت‌های متصل. آزمون گراف نیز یکتایی شناسهٔ سندِ entityها، endpointهای موجود و حفظ confidence منبع را می‌سنجد.
- این بررسی ساختاری است، نه تأیید علمی بالینی: تمام اسناد seed دارای `content_review_status: unreviewed` هستند. تطبیق snapshot/شناسه و لینک منبع، جای مرور متخصص و بررسی منبع رسمی جاری را نمی‌گیرد.
- اصلاح محلی: `buildFolderTree` چرخه را می‌شکند و `getFolderAncestorIds` مسیر والد محدود برمی‌گرداند؛ سایدبار از آن استفاده می‌کند و اسنادِ والد ناموجود را نشان می‌دهد. تست صفحه و کامپوننت با دادهٔ ساختگی چرخه و orphan را پوشش می‌دهند.
- دروازه‌ها پس از این تغییر: تست متمرکز Knowledge برابر ۱۵/۱۵ و کل Vitest برابر ۹۸ فایل/۵۷۸ تست؛ `npm run build` و typecheck موفق؛ ESLint فایل‌های مربوطه با صفر خطا و یک هشدار قدیمی `react-refresh/only-export-components`؛ `git diff --check` موفق (هشدارهای line-ending موجود). build هشدارهای پیشین static/dynamic imports و chunk بزرگ Pharmacy را دارد.
- دریافت read-only `origin/main` نشان داد HEAD شاخه و GitHub `main` هر دو `d453523fda8bd237b41b01bafdd2142350e11272` هستند؛ merge لازم نبود. شناسهٔ تب local در Chrome موجود نبود، پس screenshot/مرور بصری browser انجام نشد. هیچ دادهٔ Firestore، GitHub branch، deployment یا APK تغییر نکرد.

## شفاف‌سازی کاربرد Interactive Study (2026-09-25)

- کاربرد واقعی این صفحه «سازندهٔ تمرین از یک درس انتخاب‌شده» است: AI می‌تواند فلش‌کارت، آزمون چندگزینه‌ای، تطبیق، سناریوی مرحله‌ای، جای‌خالی، درخت تصمیم و بازی حافظه بسازد؛ کاربر پیش‌نمایش را می‌بیند و با ویجت‌ها تعامل می‌کند. این به‌تنهایی شبیه‌ساز کامل workflow داروخانه یا الگوریتم مرور Leitner نیست.
- شکاف UX در checkout محلی: دکمهٔ `Build an interactive session` پیش از بازکردن پنجرهٔ انتخاب قالب این تصور را می‌داد که همان کلیک جلسه را می‌سازد؛ تیتر `Study language` هم در پنل درس انتخاب‌شده عنوان نامربوطی بود. پنل اکنون قالب‌های قابل‌ساخت، گام بعدی، جدا ماندن درس و Leitner، و نبودِ ساخت خودکار کارت/تسک مرور را صریح می‌گوید؛ دکمه به `Choose practice formats` تغییر کرد.
- مرور read-only صفحهٔ زنده در Chrome هنوز متن قدیمی «جلسه موقت است و ذخیره نمی‌شود» را نشان داد و CYP1A2 در فهرست انتخاب‌شده بود؛ در مقابل، checkout فعلی انتخاب صریح درس و ذخیره/بازیابی جلسه را پیاده می‌کند. بنابراین deployment با رفتار checkout فعلی همگام نیست یا نسخهٔ دیگری را اجرا می‌کند؛ این نوبت هیچ تولید AI یا نوشتن Firestore روی صفحهٔ زنده انجام نداد.
- شکاف عملکردی باقی‌مانده: تاریخچهٔ مرورشدنی چند جلسه، امتیاز و تحلیل نتیجه، اتصال اختیاری به task/Leitner، و سناریوهای واقعی OTC/FRED. تولید AI و تعامل‌های ویجت در checkout تست UI/service دارند، اما فراخوانی end-to-end مولد در این مرور اجرا نشد.
- اعتبارسنجی این برش: تست صفحهٔ Interactive Study برابر ۵/۵، `npx tsc -p tsconfig.app.json --noEmit` موفق، ESLint فایل‌های صفحه/تست بدون خطا و `git diff --check` موفق (هشدار line-ending موجود). تغییر فقط در checkout محلی است؛ GitHub، Firestore زنده و deployment دست‌نخورده ماندند.

## اصلاح جلوگیری از استفادهٔ preview درس اشتباه (2026-09-25)

- باگ کد-محور: اگر پنجرهٔ سازنده بین دو درس/زبان reuse می‌شد، HTML پیش‌نمایش و قالب‌های انتخاب‌شده از ورودی قبلی باقی می‌ماندند؛ پاسخ دیررسِ درخواست AI قبلی نیز می‌توانست پس از تغییر منبع روی preview تازه بنشیند.
- پنجره اکنون با تغییر `documentId`، عنوان/متن منبع یا زبان، preview، تنظیمات سفارشی و قالب‌ها را پاک می‌کند؛ نتیجهٔ درخواست قدیمی با شمارندهٔ نسل کنار گذاشته می‌شود. تغییر فقط state رابط است و HTML یا دادهٔ جلسهٔ کاربر را دست‌کاری نمی‌کند.
- آزمون‌های regression، هم پاک‌شدن preview پس از تعویض درس و هم رد کردن پاسخ AI دیررس را پوشش می‌دهند. مرور live فقط صفحهٔ آغازین را بررسی کرده بود؛ این باگ پیش از اصلاح در browser بازتولید نشد و اصلاح در محیط زنده تأیید نشده است.
- اعتبارسنجی checkout: تست‌های تعاملی/صفحه/helper/service برابر ۳۳/۳۳ و importer برابر ۱۴/۱۴ موفق؛ `npx tsc -p tsconfig.app.json --noEmit`، ESLint فایل‌های کد درگیر و `npm run build` موفق. build هنوز هشدارهای chunk بزرگ و importهای static/dynamic دارد. تولید واقعی AI، QA تمام viewportها و اجرای importer روی Firestore زنده انجام نشد.

## دسترس‌پذیری انتخاب قالب و بررسی محلی Interactive Study (2026-09-25)

- کارت‌های قالب تمرین در `InteractiveLearningModal` قبلاً `<div onClick>` بودند؛ ماوس کار می‌کرد اما این کنترل‌ها tab-focus و activation بومی صفحه‌کلید یا حالت انتخاب قابل‌اعلام برای فناوری کمکی نداشتند. اکنون هر قالب یک دکمهٔ toggle واقعی با `aria-pressed`، focus ring و شاخص بصریِ تزئینی است.
- تست کامپوننت رفتار selected/unselected و نوع button را می‌سنجد؛ همراه تست صفحه، ۶/۶ تست InteractiveStudy/Modal موفق است. TypeScript، ESLint فایل‌های تغییرکرده و `git diff --check` نیز موفق شدند.
- مرور بصری read-only در سرور محلی `http://127.0.0.1:3002/app/interactive-study` عنوان و توضیح تازه را نشان داد. حساب/دادهٔ موجود در همین محیط صفر درسِ study-ready برگرداند و بنرِ به‌روزرسانی برنامه هم دیده شد؛ پس مسیر واقعی انتخاب درس و بازکردن modal از browser اینجا قابل تکمیل نبود. هیچ تولید AI، write یا تغییر حساب انجام نشد. تست UI با دادهٔ ساختگی تعامل toggle را پوشش می‌دهد؛ screenshot/QA از modal بازشده و viewport موبایل همچنان لازم است.
- پس از fetch فقط‌خواندنی، checkout `HEAD` و `origin/main` هم‌تراز هستند (۰ جلو/۰ عقب، `d453523`). `HEAD` و `main` مخزن Pharmacy هر دو `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` هستند و با commit ثبت‌شده در فایل گراف تولیدشده برابرند. هیچ pull/merge، push، Firestore یا deployment انجام نشد.
- بررسی build روشن کرد هشدار ۳٫۶ MB مربوط به اندازهٔ chunk دادهٔ Pharmacy است، نه دانلودِ شروع برنامه: `dist/sw.js` هیچ‌یک از `pharmacySeedData-*` یا `pharmacyLegacySeedData-*` را در precache ندارد و `globIgnores` در `vite.config.ts` مؤثر است. کل precache همچنان ۶۲۶۰٫۶۸ KiB است و چند هشدار chunk بالای ۱ MB/تقسیم import باقی‌اند؛ این یک موضوع جدا برای بهینه‌سازی شروع/آفلاین است، نه نشانهٔ cache شدن seed.
- پس از افزودن تست selector، کل Vitest دوباره اجرا شد: ۹۹ فایل و ۵۷۹ تست موفق. `npm run build` هم موفق شد و PWA تولید شد؛ هشدارهای موجود شامل static/dynamic importهای هم‌زمان، chunkهای بزرگ و اخطارهای قدیمی تست/act است. انتشار انجام نشد.

## بازخورد و اعتبارسنجی ویجت‌های Interactive Study (2026-09-25)

- بررسی رفتاری helper نشان داد بازی حافظه حالت‌های `is-matched` را پس از تشخیص جفت ذخیره نمی‌کرد و کنترل‌های تولیدشده می‌توانستند جفت‌های ناقص/تکراری داشته باشند که بازی را غیرقابل‌اتمام می‌کرد. اکنون پیشرفت جفت‌ها و پیام پایانِ دسترس‌پذیر نمایش داده و پس از resolve شدن ذخیره می‌شود؛ ساختارهای ناممکنِ memory/pair را اعتبارسنجی AI رد می‌کند.
- پایان بازی تطبیق قبلاً پیام فارسیِ ثابت داشت. زبان جلسه حالا به listenerهای preview و practice داده می‌شود تا پیام موفقیت با زبان انتخاب‌شده هماهنگ بماند. متن‌های وضعیت از `textContent` استفاده می‌کنند و `aria-live="polite"` دارند.
- اعتبارسنجی: helper برابر ۱۶/۱۶، modal برابر ۱/۱ و صفحه برابر ۵/۵ تست؛ typecheck موفق. بررسی بصری محلی صفحهٔ `/app/interactive-study` نشان داد layout تازه بارگذاری می‌شود اما حساب/محیط محلی صفر درسِ study-ready دارد، پس بازکردن modal یا تولید AI را روی آن نتوانستم به شکل end-to-end آزمایش کنم. صفحهٔ production نیز قدیمی‌تر از checkout محلی است؛ هیچ دادهٔ زنده، GitHub، انتشار یا deployment تغییر نکرد.

## پاک‌سازی HTML دانش و آماده‌سازی انتشار (2026-09-25)

- پاک‌سازی محتوای HTML به ماژول سبک و مستقل منتقل شد و در مرز ساخت/ویرایش/خواندن سند اعمال می‌شود؛ اسناد قدیمی remote و cache محلی نیز هنگام خواندن نرمال‌سازی و cache محلی مهاجرت داده می‌شود. متن ساده از HTML پاک‌شده استخراج می‌شود.
- URL منبع سند فقط وقتی نمایش داده می‌شود که یک آدرس مطلق HTTP/HTTPS بدون نام‌کاربری/گذرواژه باشد؛ ارجاع‌های شواهد بازبینی همچنان محدودتر و فقط HTTPS هستند. پیوند منبع در Reader و Interactive Study از همان اعتبارسنج استفاده می‌کند.
- تست‌های ساختگی پوشش تزریق script/event handler، طرح `javascript:`, credential داخل URL و مهاجرت cache legacy را اضافه کردند؛ هیچ دادهٔ زنده‌ای تغییر نکرد.
- اعتبارسنجی نهایی checkout: Vitest برابر ۹۹ فایل و ۵۸۸ تست موفق؛ `npm run typecheck` و ESLint فایل‌های تغییرکرده موفق؛ `npm run build` موفق. هشدارهای قبلی دربارهٔ chunk بزرگ Pharmacy و importهای static/dynamic باقی‌اند.
- صفحهٔ production در Chrome هنوز متن قدیمی Interactive Study را نشان می‌داد؛ پس از push به `main` لازم است وضعیت deployment و صفحهٔ زنده جداگانه تأیید شود. این یادداشت وضعیت مشاهده‌شده پیش از انتشار را ثبت می‌کند.

## QA تعاملی زنده و دسترسی Outliner در موبایل (2026-09-25)

- در صفحهٔ production `/app/interactive-study` تعداد ۳۷۵ درس نمایش داده شد. انتخاب آزمایشی CYP1A2 وضعیت UI را تغییر داد، اما بررسی جلسهٔ ذخیره‌شده با پیام «جلسهٔ ذخیره‌شده بررسی نشد» شکست خورد. هنوز امکان شروع ساخت تمرین از همان صفحه وجود داشت؛ برای جلوگیری از ریسک هزینه/ارسال محتوا و نوشتن داده، تولید یا ذخیرهٔ AI انجام نشد.
- هم‌زمان کنسول Chrome خطای سهمیهٔ روزانهٔ رایگان Firestore را برای `subscribeFolders` و `subscribeTags` ثبت کرد. این شاهد، محدودیت واقعی خواندن Firestore را ثابت می‌کند؛ ارتباط علّی مستقیم آن با شکست query جلسه در این بارِ خاص لاگ نشد و باید با دسترسی مناسب به سهمیه/لاگ سرویس تأیید شود. بررسی نباید با refresh یا انتخاب‌های مکرر که خواندن بیشتری ایجاد می‌کنند ادامه پیدا کند.
- در viewport باریک، کاربرد صفحه در ورود اولیه هنوز به‌قدر کافی راهنما نیست: تمرین‌ها فقط پس از انتخاب درس و بازکردن پنجرهٔ قالب‌ها ملموس می‌شوند، و پنل جلسه زیر فهرست درس قرار می‌گیرد. هفت قالب و جدایی از Leitner در پنجرهٔ قالب‌ها روشن است؛ بازآرایی onboarding و نمایش واضح جلسه‌های اخیر/قابل ادامه نیاز به مرحلهٔ جداگانه دارد.
- در checkout محلی، کنترل «گسترش/جمع‌کردن همه» که پیش‌تر در عرض‌های کمتر از `sm` پنهان بود اکنون از منوی فشرده و نام‌گذاری‌شدهٔ «گزینه‌های نقشهٔ ذهنی» در دسترس است. اصلاح همراه آن باعث می‌شود «جمع‌کردن همه» ریشهٔ پایگاه دانش را باز نگه دارد و فقط شاخه‌های پایین‌تر را ببندد؛ تست با fixture این رفت‌وبرگشت را تأیید می‌کند.
- اعتبارسنجی محلی: تست Outliner برابر ۳/۳، تست‌های Interactive Study/Modal/Service برابر ۱۷/۱۷، TypeScript و ESLint فایل‌های تغییرکرده موفق و `git diff --check` موفق. مرور بصری محلی در viewport باریک، بازشدن منو را تأیید کرد؛ محیط محلی صفر سند داشت، بنابراین رفتار روی دادهٔ واقعیِ شاخه‌ها فقط با fixture پوشش داده شد.
- Live QA فقط انتخاب یک درس را تغییر داد و هیچ تمرینی یا رکوردی در حساب ذخیره نشد.

## بازیابی پس از خطای بررسی جلسهٔ ذخیره‌شده (2026-09-25)

- باگ رابط: سرویس خطای واقعیِ بارگذاری پیش‌نویس را برمی‌گرداند، اما صفحه پیام عمومی ثابت نشان می‌داد و دکمه‌ای برای retry نداشت؛ بنابراین علت شکست برای کاربر پنهان می‌ماند.
- صفحه اکنون پیام امن و ثابت را حفظ می‌کند، جزئیات فنی خطا را پشت disclosure و با سقف ۳۰۰ نویسه نشان می‌دهد، و retry را فقط با اقدام صریح کاربر اجرا می‌کند. شمارندهٔ درخواست، پاسخ دیررسِ سند/زبان قبلی را از تغییر state جلسهٔ جدید بازمی‌دارد.
- تست صفحه ۶/۶ موفق شد؛ تست regression نشان می‌دهد `RESOURCE_EXHAUSTED` قابل مشاهده است، retry جلسهٔ ذخیره‌شده را بازیابی می‌کند و خودش هیچ writeای اجرا نمی‌کند. TypeScript، ESLint فایل‌های تغییرکرده، `git diff --check` و `npm run build` نیز موفق‌اند.
- این اصلاح خطای UX/بازیابی را پوشش می‌دهد، نه افزایش سهمیه یا رفع ریشهٔ خطای Firestore. اگر محدودیت روزانه ادامه داشته باشد retry هم ممکن است شکست بخورد؛ برای رفع آن باید وضعیت quota/صورتحساب پروژه جداگانه بررسی شود. در این دور دادهٔ زنده، تنظیمات billing، تولید AI و APK تغییر نکرد.

## ایمنی و صحت ساختار تمرین‌های تعاملی (2026-09-25)

- مخزن Pharmacy روی `main` با commit `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` تازه‌سازی و بدون تغییر محلی بررسی شد. مولد کارت‌های آن از مدل صریح دوزبانه برای همهٔ فیلدها استفاده می‌کند؛ این با مولد HTML تعاملی ARSHNAZ که یک زبان در هر جلسه می‌سازد، هم‌ارز نیست و باید در نقشهٔ parity باقی بماند.
- پرامپت مولد تعاملی اکنون صریحاً به منبع درس محدود است، ادعا/دوز/قانونِ غایب از متن را نسازد، منبع را به‌عنوان دستور سیستم تلقی نکند، و citation یا بررسی راهنمای جاری را جعل نکند. این قید احتمال خطا را کاهش می‌دهد اما جایگزین بازبینی علمی انسان یا اعتبارسنجی معنایی پاسخ AI نیست.
- اعتبارسنجی خروجی اکنون برای هر MCQ حداقل دو گزینه و دقیقاً یک پاسخ درست می‌خواهد و تمام مقصدهای دکمه‌های سناریو/درخت تصمیم را بررسی می‌کند؛ handler مقصد را با مقایسهٔ دقیق مقدار attribute پیدا می‌کند تا شناسهٔ دارای نویسه‌های CSS صفحه را خراب نکند یا کلیک نامعتبر همهٔ گام‌ها را پنهان نسازد.
- تست‌های regression این موارد را با HTML ساختگی پوشش می‌دهند؛ هیچ درس زنده‌ای برای تولید AI انتخاب نشد و هیچ session یا رکوردی نوشته نشد. هم‌ارزی دوزبانه، تاریخچهٔ چندجلسه‌ای، کیفیت بالینی خروجی و رفتار تولید واقعی همچنان نیازمند مرحلهٔ جدا و ارزیابی انسانی‌اند.

## اصلاح سناریوی توکن eScript گم‌شده و Active Script List (2026-09-25)

- محتوای سند `doc-scenario-admin-admin-lost-escript-mysl` با شناسهٔ پایدار، پوشه، نشانی منبع Pharmacy، رابطهٔ گراف و وضعیت `unreviewed` حفظ شد؛ هیچ `content_review_evidence` ساختگی اضافه نشد.
- آموزش اکنون ASL/MySL را راهکار مدیریت توکن برای مصرف‌کنندهٔ ثبت‌نام‌شده و دارای دسترسی مجاز معرفی می‌کند، نه جست‌وجوی عمومی برای هر پیامک حذف‌شده. مسیر ثبت ASL، احراز هویت، IHI/HI Service و تأیید دسترسی جدا از بازیابی نسخه توضیح داده شده است. برای توکن نسخهٔ اصلیِ گم‌شده مسیر نسخه‌نویس و برای توکن تکرار مسیر داروخانهٔ صادرکننده آمده؛ دیسپنس پیش از بازیابی و اعتبارسنجی توصیه نمی‌شود.
- ارجاع‌ها به راهنمای ADHA دیسپنسر، راهنمای مصرف‌کننده و چارچوب حریم خصوصی دولت استرالیا افزوده شدند. عنوان منبع در هر دو زبان ملی استرالیا است؛ یک تست regression اختلاف عنوان فارسیِ باقی‌مانده با عنوان انگلیسی را نیز می‌گیرد. SafeScript همچنان عنوان NSW درست خود را حفظ می‌کند.
- Pharmacy از commit `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` فقط خوانده شد و تغییر نکرد. بازتولید خروجی‌ها شمارها را حفظ کرد: ۴۳۲ سند، ۳۵ کارت، ۳۴ پوشه و گراف با ۴۴۲ موجودیت/۳۲۲ نگاشت/۱۲۰ موجودیت بی‌نگاشت/۵۴۶ رابطه.
- پس از `git fetch origin main`، checkout ARSHNAZ در `04dd182f887031772d547a7cf7ac4aa8e2dbf865` با `origin/main` و checkout Pharmacy در `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` با `origin/main` برابر بودند؛ Pharmacy clean ماند. فقط fetch انجام شد؛ merge، commit، push، Firestore و deployment انجام نشد.
- تست `pharmacyImportService` برابر ۱۴/۱۴، typecheck و `npm run build` موفق‌اند. build همچنان هشدارهای importهای هم‌زمان static/dynamic و chunk بزرگ دادهٔ Pharmacy را می‌دهد. محتوا برای استفادهٔ بالینی بازبینی واجدصلاحیت نشده و همچنان `unreviewed` است؛ هیچ دادهٔ Firestore زنده، GitHub، deployment یا APK تغییر نکرد.

## بازبینی سناریوی برفک واژینال در بارداری (2026-09-25)

- در Pharmacy `data/scenarios/clinicalScenarios.ts`، سناریوی `thrush-triage` (بیمار باردار ۱۴ هفته‌ای) فلوکونازول خوراکی را «منع مطلق» می‌خواند و خطر سقط/ناهنجاری را برای همهٔ موارد قطعی بیان می‌کند؛ همچنین استفاده از اپلیکاتور را به‌طور کلی منع کرده است. این گزاره‌ها بیش از آنچه منابع رسمی بررسی‌شده پشتیبانی می‌کنند قطعی‌اند.
- overlay محلی ARSHNAZ برای تأیید تشخیصِ نخستین بروز توسط GP/ماما پیش از درمان، ترجیح درمان واژینال و مشاورهٔ حرفه‌ای پیش از فلوکونازول اصلاح شد. MotherSafe NSW، کلوتریمازول واژینال ۱٪ یا پِساری ۱۰۰ میلی‌گرم را شبانه به‌مدت ۶ روز ذکر می‌کند؛ اپلیکاتور را با احتیاط مجاز می‌داند و فلوکونازول خوراکی را پس از سه‌ماههٔ اول، در صورت شکست/عدم تحمل درمان موضعی، خط دوم می‌خواند. Healthdirect نیز تشخیص با پزشک/ماما در بارداری و پرهیز از فلوکونازول مگر با توصیهٔ پزشک را بیان می‌کند؛ پایگاه TGA زمینهٔ دسته‌بندی D و هشدارهای وابسته به دوز/سه‌ماهه را ثبت می‌کند.
- منبع Pharmacy، شناسهٔ سند `doc-scenario-clinical-thrush-triage`، پوشه، لینک‌های داخلی و `content_review_status: unreviewed` حفظ می‌شوند؛ overlay، graph title و آزمون‌ها محلی‌اند و منبع اصلی Pharmacy و `content_review_evidence` دستکاری/جعل نمی‌شوند. ارجاع‌های NSW و ملی با تاریخ منبع و تاریخ دسترسی در هر دو زبان نمایش داده می‌شوند.
- منابع مشاهده‌شده در ۲۵ سپتامبر ۲۰۲۶: [MotherSafe NSW, Thrush in pregnancy (July 2024)](https://www.seslhd.health.nsw.gov.au/sites/default/files/groups/Royal_Hospital_for_Women/Mothersafe/documents/Factsheets/ThrushinPregnancyJuly152024.pdf)، [Healthdirect, Vaginal thrush (reviewed November 2025)](https://www.healthdirect.gov.au/vaginal-thrush)، [Healthdirect, Medicines during pregnancy (reviewed June 2024)](https://www.healthdirect.gov.au/medicines-during-pregnancy)، [TGA, Prescribing medicines in pregnancy database (updated 19 May 2026)](https://www.tga.gov.au/resources/health-professional-information-and-resources/australian-categorisation-system-prescribing-medicines-pregnancy/prescribing-medicines-pregnancy-database).
- این یک اصلاح تحریریهٔ مستند است، نه تأیید کلینیکی: سند باید `unreviewed` بماند تا مالک بالینی و بازبین واجدصلاحیت، متن فارسی/انگلیسی و نسخه‌های مرجع را تأیید کنند. هیچ انتشار production، push، APK یا نوشتن Firestore در این مرحله انجام نمی‌شود.
- آزمون `pharmacyImportService` پس از اصلاح ۱۴/۱۴ موفق شد؛ شناسه، پوشه، provenance، وضعیت بازبینی‌نشده، نبود evidence ساختگی، برابری فارسی/انگلیسی، لینک منابع و رد عبارت‌های قطعیِ قبلی را می‌سنجد. `node --check` برای هر دو مولد، TypeScript، `git diff --check` و `npm run build` نیز موفق‌اند.
- build همچنان هشدارهای قدیمی static/dynamic import و chunk بزرگ seed دارویی (~۳٫۸ MB، gzip ~۴۳۶ KB) را می‌دهد؛ PWA precache برابر ۱۶۰ فایل و ۶۲۷۱ KiB است. این اصلاح clinical text، refactor یا رفع ریسک حجم bundle نیست.
- مخزن Pharmacy روی `5b4f7d2443a3ed97aea752c1d0d18583ce6d0067` clean ماند. تغییرهای ARSHNAZ فقط در worktree محلی‌اند؛ هیچ commit، push، deployment، Firestore write یا APK در این دور انجام نشد.

## بازبینی تعارض منبع در سناریوی آبله‌مرغان کودک (2026-09-25)

- در سناریوی Pharmacy با شناسهٔ `chickenpox-advisory`، توصیهٔ مطلق دربارهٔ منع NSAID، خطر قطعی نکروز فاشیا برای هر مصرف، و منع کلی حمام/روغن از شواهد رسمی بازبینی‌شده فراتر می‌رفت. overlay محلی ARSHNAZ همان شناسهٔ سند، پوشه، URL منبع و پیوند گراف را حفظ می‌کند، تعارض را به فارسی و انگلیسی آشکار می‌سازد و وضعیت را `unreviewed` نگه می‌دارد؛ `content_review_evidence` ساختگی تولید نشده است.
- مقایسهٔ زندهٔ منابع در ۲۵ سپتامبر ۲۰۲۶ یک اختلاف واقعی نشان داد: [Healthdirect Australia، Chickenpox (بازبینی مهٔ ۲۰۲۵)](https://www.healthdirect.gov.au/amp/article/chickenpox) پرهیز از NSAIDها مانند ایبوپروفن را توصیه می‌کند و افزایش خطر عارضه را ذکر می‌کند. [راهنمای بالینی RCH Melbourne](https://www.rch.org.au/clinicalguide/guideline_index/Chickenpox_varicella/) از NSAIDها به‌دلیل خطر بیشتر عفونت تهاجمی GAS پرهیز می‌دهد؛ آخرین به‌روزرسانی صفحه ژوئیهٔ ۲۰۲۱ است. برگهٔ والدین RCH می‌گوید ایبوپروفن ندهید؛ این صفحه «بازبینی ژوئیهٔ ۲۰۲۱» و «به‌روزرسانی ژوئیهٔ ۲۰۲۵» را جداگانه ثبت می‌کند. در مقابل، [Sydney Children’s Hospitals Network در NSW](https://www.schn.health.nsw.gov.au/factsheets/chickenpox)، آخرین به‌روزرسانی ۱۳ آوریل ۲۰۲۶، پاراستامول یا ایبوپروفن را برای تب/درد فهرست می‌کند؛ همان برگه خود را اطلاعات عمومی می‌داند، نه توصیهٔ اختصاصی برای یک کودک.
- [Better Health Channel Victoria](https://www.betterhealth.vic.gov.au/health/conditionsandtreatments/chickenpox) (بازبینی ۲۳ اکتبر ۲۰۲۳) لوسیون تسکین‌دهنده و حمام جو دوسر کلوئیدی را برای خارش ذکر می‌کند. این منبع از منع مطلق همهٔ حمام‌ها یا ادعای اینکه همهٔ روغن‌ها گرما را حبس می‌کنند/عفونت می‌سازند پشتیبانی نمی‌کند؛ اما این به‌تنهایی ایمنی همهٔ فرآورده‌ها را ثابت نمی‌کند.
- به‌خاطر اختلاف Healthdirect/RCH با برگهٔ NSW، متن اکنون از توصیهٔ بی‌قید ایبوپروفن خودداری می‌کند و حل انتخاب درمان را به راهنمای جاریِ محل و بازبین بالینی واجدصلاحیت می‌سپارد. دو منبع از نظر نوع و کارکرد یکسان نیستند (راهنمای بالینی در برابر برگهٔ عمومی بیمارستانی)؛ بنابراین نباید اختلاف را با انتخاب گزینشی یک URL «حل‌شده» گزارش کرد.
- اصلاح تاریخ منبع RCH در مولد، افزودن پیوند مستقیم راهنمای بالینی RCH و تست رگرسیون برای تاریخ‌های منبع و پیوند آن انجام شد. شناسهٔ سند، پوشه، provenance، رابطهٔ گراف و وضعیت بازبینی حفظ می‌شوند. خروجی‌های مولد باید بازتولید و آزمون‌های متمرکز پس از اصلاح اجرا شوند.
- این اصلاح تحریریه‌ای هنوز تأیید بالینی نیست. پیش از انتشار clinical owner/reviewer باید متن دو‌زبانه، منبع مرجع/حوزهٔ قضایی و تاریخ بازبینی بعدی را تأیید و ثبت کند؛ تا آن زمان production، GitHub `main` و Firestore زنده دست‌نخورده می‌مانند.

## درخواست صریح برای تولید کارت هوشمند (2026-09-25)

- ممیزی تازهٔ `AiQuestionGeneratorModal` نشان داد که بازکردن پنجره، بدون زدن دکمهٔ تولید، `generateQuestionsFromText` را اجرا می‌کرد؛ این می‌توانست متن درس را ناخواسته برای سرویس آنلاینِ تنظیم‌شده بفرستد و مصرف AI ایجاد کند.
- اجرای خودکار حذف شد. عنوان/متن/تنظیمات تولید و امکان افزوده‌شدن زمینهٔ شخصی‌سازیِ opt-in پیش از اقدام شفاف توضیح داده می‌شوند؛ در حالت آفلاین پردازش محلی می‌ماند. ارسال تنها با انتخاب صریح «تولید» شروع می‌شود و کارت‌ها پیش از ذخیره همچنان قابل بازبینی و ویرایش‌اند.
- تست رگرسیون بازشدن و rerender بدون درخواست را ثابت می‌کند و سپس فقط پس از کلیک تولید را می‌سنجد. درخواست در حال اجرا با بستن پنجره، تغییر سند/پوشه/منبع یا تغییر تنظیمات لغو می‌شود؛ پاسخ دیررس هم اجازهٔ جایگزین‌کردن کارت‌های سند جدید را ندارد. سیگنال لغو تا سرویس AI عبور داده می‌شود و در صورت لغو، استخراج آفلاین ناخواسته اجرا نمی‌شود. تست modal برابر ۶/۶ و تست helper برابر ۶/۶ موفق شدند؛ TypeScript، ESLint فایل‌های تغییرکرده و build نهایی هم موفق شدند. build همچنان هشدار chunk بزرگ seed داروخانه (حدود ۳٫۸ MB پیش از gzip)، precache برابر ۶۲۷۵٫۵۰ KiB و برخی importهای static/dynamic را نشان می‌دهد.
- مرور محلی `/app/knowledge` با HTTP 200 بالا آمد، اما حساب محلی هیچ سندی نداشت؛ پس modal از خود صفحه در مرورگر end-to-end باز نشد. مسیر UI آن با React Testing Library آزموده شد. هیچ AIای فراخوانی نشد، هیچ Firestore زنده‌ای نوشته نشد و GitHub، production یا APK تغییر نکرد.
- پشتیبانی دوزبانهٔ کارت‌ها در ادامه همین برش پیاده شد: چهار فیلد اختیاری فارسی/انگلیسی، انتخاب زبان مرورِ مستقل و ذخیره‌شونده در همین مرورگر، fallback سازگار برای کارت‌های قدیمی، و نمایش زبان انتخابی در مرور، فهرست، Outliner و Mind Map. جست‌وجو نیز متن هر دو زبان را می‌گردد و فرم‌های تولید هوشمند، دستی، ایجاد و ویرایش کارت ترجمه‌ها را می‌پذیرند. آزمون‌های هدفمند ۶ فایل (۵۰/۵۰)، TypeScript، ESLint فایل‌های کد تغییرکرده، و build تولیدی موفق شدند.
- ترجمهٔ پیشنهادی مدل به‌عنوان محتوای تأییدشدهٔ دارویی تلقی نمی‌شود؛ ترجمه‌ها پیش از ذخیره قابل بازبینی/ویرایش‌اند و کارت‌های قدیمی بدون دادهٔ دوزبانه، متن موجود را با پیام «ترجمه موجود نیست؛ متن اصلی نمایش داده می‌شود» نشان می‌دهند. داده‌های Firestore مهاجرت اجباری ندارند و فیلدهای قبلی دست‌نخورده می‌مانند.
- build فقط هشدارهای غیرمسدودکنندهٔ موجود دربارهٔ importهای static/dynamic و chunk seed داروخانه (حدود ۳٫۸۴ MB پیش از gzip) را نشان داد؛ PWA هم ۱۶۰ فایل با precache حدود ۶٫۲۹ MB تولید کرد.
