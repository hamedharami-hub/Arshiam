# گزارش تجمعی اصلاحات Android و Web تا نسخهٔ ۱.۲.۱

مبنای تغییر: commit `54d3968`. شاخه: `codex/android-auth-widget-fixes`.

## تغییرات

- ورود Google در Android از Credential Manager و دکمهٔ رسمی Sign in with Google استفاده می‌کند. توکن Google فقط به Firebase Auth تحویل می‌شود؛ هویت محلی یا ورود ساختگی وجود ندارد. ورود وب همچنان popup است.
- همان Web OAuth client ID موجود در پیکربندی پروژه استفاده می‌شود. هیچ client secret یا فایل کلید امضا اضافه نشده است.
- ویجت برای خواندن Firestore از مسیر خصوصی کاربر در URL استفاده می‌کند و پاسخ JSON آرایه‌ای را می‌خواند.
- Firebase refresh token فقط از پل داخلی به EncryptedSharedPreferences منتقل می‌شود تا کار پس‌زمینه توکن کوتاه‌مدت را تمدید کند. این تغییر، نگهداری اعتبار بلندمدت در بخش بومی را اضافه می‌کند؛ خروج یا تغییر حساب آن را پاک می‌کند. لاگ پل خاموش است و فایل‌های ویجت از backup و device transfer مستثنا هستند.
- کارهای دستی و دوره‌ای دریافت اطلاعات در یک صف اجرا می‌شوند. نتایج متعلق به نسل قبلی نشست یا قبل از تغییر تازهٔ داده کنار گذاشته می‌شوند.
- دادهٔ آفلاین دارای عملیات ارسال‌نشده با نتیجهٔ قدیمی سرور جایگزین نمی‌شود. برای ارسال این عملیات باید خود برنامه باز شود.
- تاریخ‌های UTC به منطقهٔ زمانی گوشی تبدیل می‌شوند، هر دو مسیر سه عنوان را نشان می‌دهند، دکمهٔ یادداشت‌ها مسیریابی می‌شود و متن ثابت وضعیت باغچه حذف شده است.
- آخرین زمان داده و وضعیت خطا در ویجت دیده می‌شود. Android زمان اجرای WorkManager را تعیین می‌کند؛ اجرای دقیق سر ساعت تضمین نمی‌شود.
- ساخت‌ها از npm lockfile و Java 21 استفاده می‌کنند. نسخهٔ APK برابر 1.1.2 و versionCode برابر 4 است.

## پیش‌نیاز واقعی ورود Google

در Google/Firebase باید OAuth client اندروید برای `life.arshnaz.app` با SHA-1 گواهی همان APK ثبت شده باشد و Google provider فعال باشد. Web OAuth client ID باید متعلق به همان پروژه باشد. وجود کد یا موفقیت build صحت این تنظیمات خارجی را اثبات نمی‌کند. کلید امضا یا رمز آن را داخل repository قرار ندهید.

برای دریافت SHA همان فایل:

`apksigner verify --print-certs app-debug.apk`

## ساخت و تست

```text
npm ci
npm test
npm run build
npx cap sync android
cd android
gradlew :app:testDebugUnitTest :app:assembleDebug
```

به JDK 21 و Android SDK نیاز است. در Windows در صورت قفل موقت Gradle cache می‌توان از `--no-watch-fs --max-workers=1` استفاده کرد.

## آزمون روی گوشی

1. نصب روی نسخهٔ قبلی با همان گواهی؛ برای حل اختلاف امضا، دادهٔ برنامه را حذف نکنید.
2. ورود Google، انتخاب حساب، لغو ورود و بازگشت به برنامه.
3. ساخت چهار تسک امروز، انجام یکی و بررسی تعداد و سه عنوان ویجت.
4. بازکردن یادداشت‌ها، تمرکز، چک‌این و تسک جدید از ویجت.
5. دریافت دستی پس از تغییر داده از دستگاه دیگر و دریافت پس از گذشت اعتبار یک‌ساعتهٔ توکن.
6. اینترنت قطع: نمایش خطا و حفظ آخرین داده؛ اینترنت وصل: بازیابی دریافت.
7. خروج و تعویض حساب: هیچ عنوان یا نتیجهٔ دیررس حساب قبلی نباید نمایش داده شود.
8. تسک نزدیک نیمه‌شب سیدنی و تغییر روز/منطقهٔ زمانی.

هیچ ورود واقعی، درخواست خواندن دادهٔ خصوصی یا تنظیمات حساب Google با تست‌های واحد شبیه‌سازی‌شده تأیید نمی‌شود.

## نتیجهٔ بررسی اولیهٔ ۱.۱.۲ در ۱۰ سپتامبر ۲۰۲۶

- ساخت production وب و همگام‌سازی دارایی‌ها با Android موفق بود.
- ۵۹ تست وب موفق شد؛ ۹ تست جدید مربوط به ورود Google و نشست/دادهٔ ویجت است.
- چهار تست اختصاصی Java برای ویجت و یک تست نمونهٔ قبلی موفق شد.
- ساخت نهایی `:app:testDebugUnitTest :app:assembleDebug` موفق بود.
- امضای APK با apksigner تأیید شد؛ تمام فایل‌های وب داخل APK با خروجی نهایی dist یکسان بودند.
- ESLint فایل‌های جدید TypeScript و بررسی YAML workflowها موفق بود.
- TypeScript کل پروژه هنوز ۵۶ خطا دارد. همان خطاها روی commit پایه با همان وابستگی‌ها بازتولید شدند؛ خطای اضافه‌ای از این تغییرات وجود ندارد.
- بررسی الگوهای رایج private key و token در فایل‌های تغییرکرده موردی پیدا نکرد؛ این بررسی محدود، ممیزی کامل امنیتی نیست.
- آزمون روی گوشی/شبیه‌ساز، ورود واقعی Google، تمدید توکن واقعی و دریافت خصوصی Firestore انجام نشده است.
- GitHub Actions جدید هنوز اجرا نشده و نسخهٔ امضاشدهٔ انتشار تولید نشده است.

### فایل ساخته‌شده

`artifacts/ARSHNAZ-1.1.2-debug.apk`

SHA-256 فایل:
`2193d9fdd15642db5e9a7e3543dee53bc8884766a0537db96781709df4a2f6c8`

SHA-1 گواهی Debug جدید:
`db665e841ad598744063e2743cc532fb7d51787d`

SHA-1 گواهی APK ارسالی مالک:
`bbd49ff9f58e43dbeb413e114ea122d7f02e4f55`

این دو گواهی متفاوت‌اند؛ فایل جدید روی نصب قبلی به‌عنوان update پذیرفته نمی‌شود.
برای حفظ داده‌های محلی، برنامهٔ قبلی را حذف نکنید. ساخت update به همان کلید امضای قبلی نیاز دارد.
گواهی Debug جدید نیز برای ورود Google باید در تنظیمات OAuth اندروید برای package
`life.arshnaz.app` ثبت شده باشد. فقط fingerprint گواهی عمومی است؛ فایل کلید و رمز نباید ارسال یا commit شوند.

مراجع طراحی: [ورود Android با Google](https://firebase.google.com/docs/auth/android/google-signin)،
[تمدید توکن Firebase](https://firebase.google.com/docs/reference/rest/auth#section-refresh-token)،
[Firestore runQuery](https://firebase.google.com/docs/firestore/reference/rest/v1/projects.databases.documents/runQuery).

## اصلاح تکمیلی ۱.۱.۳ پس از گزارش مالک

- علت خرابی ساخت نمای ویجت بازتولید شد: `RemoteViews.apply` عنصر `android.view.View` را نمی‌پذیرد. آزمون Robolectric روی API 28 و 35 با XML نسخهٔ ۱.۱.۲ هر دو با `Class not allowed to be inflated android.view.View` شکست خوردند. جداکننده به `ImageView` سازگار تغییر کرد.
- یک مسابقهٔ زمانی برطرف شد: آخرین دادهٔ تسک‌ها تا آماده‌شدن نشست بومی نگه داشته و سپس ارسال می‌شود. در خروج یا تعویض حساب دادهٔ قبلی کنار گذاشته می‌شود.
- پیام لغو گوگل دیگر قطعی فرض نمی‌کند که کاربر پنجره را بسته است. نوع خطای ارائه‌دهنده بدون توکن یا متن خام خطا به پاسخ پل اضافه شد. این تغییر به‌تنهایی رفع ورود نیست.
- کنسول واقعی Firebase پروژهٔ `gen-lang-client-0845891098` بررسی شد: Google provider فعال است، ولی در Your apps فقط Web app موجود است و Android app ثبت نشده است. ثبت package و SHA نیاز به تأیید مالک دارد و تا دریافت آن انجام نمی‌شود.
- نسخهٔ جدید `1.1.3`، کد نسخه `5` است و از همان کلید Debug محلی نسخهٔ ۱.۱.۲ استفاده می‌کند؛ کلید یا رمز در مخزن قرار نمی‌گیرد.
- ۶۰ آزمون وب، شامل آزمون جدید رسیدن داده پیش از آماده‌شدن نشست، موفق شدند؛ build وب، همگام‌سازی Capacitor و ESLint فایل‌های تغییرکرده نیز موفق شدند.
- هیچ گوشی به adb متصل نیست؛ آزمون Robolectric جای تأیید روی لانچر گوشی، ورود واقعی گوگل یا دریافت واقعی Firestore را نمی‌گیرد.
- ساخت نهایی APK و هر ۷ آزمون Android موفق شدند: دو آزمون ساخت واقعی RemoteViews روی API 28/35، چهار آزمون داده و یک آزمون نمونه. همان دو آزمون نمای ویجت که پیش از اصلاح شکست خورده بودند اکنون موفق‌اند.
- فایل خروجی: `artifacts/ARSHNAZ-1.1.3-debug.apk`؛ امضا با apksigner تأیید شد و با نسخهٔ ۱.۱.۲ یکسان است.
- SHA-256 فایل: `697b9f3500f152e27d07fc2bcd9a6084da25dea9de1eacefa436ad2c6d63dbfd`.

### ثبت Firebase پس از تأیید صریح مالک

در ۱۰ سپتامبر ۲۰۲۶ مالک ثبت همین Android app و امضای APK را تأیید کرد. برنامهٔ `ARSHNAZ Android` با package `life.arshnaz.app` در پروژهٔ Iran1 ثبت شد؛ App ID برابر `1:627668608593:android:7a85c2bf10d99b11887a70` است. هر دو اثرانگشت SHA-1 و SHA-256 گواهی APK ۱.۱.۳ ذخیره و در جدول کنسول تأیید شدند.

Google Cloud نیز ایجاد خودکار OAuth client اندروید را نشان داد: `627668608593-htqbam1eedagm6u9bil6lg1ukb5e9kmt.apps.googleusercontent.com`. Web client موجود همان شناسهٔ استفاده‌شده در برنامه است و تغییر نکرد. این ثبت سمت سرویس انجام شد و برای همین تغییر نیازی به بازسازی APK ۱.۱.۳ نیست. ورود واقعی روی گوشی هنوز باید توسط مالک آزمایش شود؛ ایجاد تنظیمات به‌تنهایی اثبات ورود موفق نیست.

مرجع محدودیت ساخت نمای ویجت: [Android RemoteViews](https://developer.android.com/reference/android/widget/RemoteViews).

## نسخهٔ ۱.۲.۰ — تجربهٔ بومی کامل‌تر Android

- پنج ورودی ویجت در انتخاب‌گر Android وجود دارد: ویجت قبلی، امروز، فردا، هفت روز آینده و نمای فشرده.
- هر نمونهٔ ویجت تنظیم مستقل دارد: بازهٔ زمانی، پوستهٔ روشن/تیره، نمایش کارهای انجام‌شده، فقط اولویت بالا و اندازهٔ فهرست. چند نمونه با تنظیمات متفاوت می‌توانند هم‌زمان روی صفحه باشند.
- ردیف‌های ویجت به خود تسک باز می‌شوند؛ دکمه‌های افزودن، تازه‌سازی و تنظیمات نیز مسیر مستقل دارند. دادهٔ ویجت به شناسهٔ حساب واردشده محدود است.
- فیدبک لمسی بومی برای لمس، انتخاب، موفقیت، هشدار و خطا اضافه شده و از تنظیمات Android قابل خاموش‌کردن است.
- هماهنگ‌کنندهٔ حرکت لمسی واحد اضافه شده تا حرکت از لبهٔ راست منو را باز کند، حرکت داخل منو آن را ببندد و حرکت افقی در بدنه بین امروز، فردا، هفت روز و صندوق ورودی جابه‌جا شود؛ کنترل‌ها، پنجره‌ها و اسکرول عمودی مزاحم این حرکت نمی‌شوند.
- دکمهٔ Back ابتدا پنجره/منو را می‌بندد، سپس تاریخچه را برمی‌گرداند و در صفحهٔ اصلی برای خروج دو بار فشار لازم دارد.
- یادآورها با AlarmManager خود Android زمان‌بندی می‌شوند و پس از بسته‌شدن WebView، راه‌اندازی مجدد دستگاه و تغییر ساعت/منطقهٔ زمانی بازسازی می‌شوند. در نبود مجوز exact alarm، مسیر امنِ غیر‌دقیق استفاده می‌شود.
- اعلان یادآور خصوصی است و «۱۰ دقیقه بعد» و «باز کردن تسک» دارد. کار انجام‌شده، حذف‌شده، خروج حساب یا خاموش‌کردن اعلان‌ها زمان‌بندی مرتبط را لغو می‌کند.
- پنل اختیاری و کم‌صدا در notification shade کارهای امروز/فردا را با دکمه‌های قبلی، بعدی و بازکردن نمایش می‌دهد. Android امکان carousel دلخواه با swipe داخل اعلان را تضمین نمی‌کند؛ بنابراین کنترل‌های استاندارد و پایدار اعلان استفاده شده‌اند.
- Quick Settings tile برای بازکردن سریع «تسک جدید» اضافه شده است؛ کاربر آن را از بخش ویرایش پنل تنظیمات سریع Android به نوار خود اضافه می‌کند.
- صفحهٔ تنظیمات Android وضعیت مجوز اعلان، مجوز exact alarm، تعداد یادآورهای زمان‌بندی‌شده، پنل اعلان، حرکت‌های لمسی و فیدبک لمسی را نمایش می‌دهد.
- نسخهٔ برنامه `1.2.0` و `versionCode` برابر `6` است. با همان امضای Debug نسخهٔ ۱.۱.۳ ساخته می‌شود و به‌عنوان update روی آن نصب می‌شود.

### نتیجهٔ ساخت ۱.۲.۰ در ۱۰ سپتامبر ۲۰۲۶

- build تولید وب و همگام‌سازی Capacitor موفق بود.
- ۷۰ تست TypeScript/React و ۲۸ تست Android بدون شکست یا ردشدن موفق شدند.
- Android Lint پس از اصلاح چهار خطای پیدا‌شده با صفر خطا تمام شد؛ هشدارهای غیرمسدودکنندهٔ قدیمی پروژه همچنان گزارش می‌شوند.
- TypeScript کل پروژه خطاهای قدیمی commit پایه را دارد، اما تغییرات ۱.۲ خطای تازه‌ای اضافه نکرد و دو ناسازگاری قدیمی نوع haptic را رفع کرد.
- APK از نظر package، نسخه و ثبت اجزای Android بررسی شد: `life.arshnaz.app`، نسخهٔ `1.2.0`، کد `6` و هر پنج provider ویجت داخل manifest نهایی وجود دارند.
- امضای APK با طرح v2 تأیید شد. SHA-1 گواهی `db665e841ad598744063e2743cc532fb7d51787d` است؛ همان گواهی ثبت‌شده در Firebase و استفاده‌شده برای نسخهٔ ۱.۱.۳.
- فایل خروجی: `artifacts/ARSHNAZ-1.2.0-debug.apk`
- SHA-256 فایل: `64a310fa1dc99194efeced56b6756723ae0a9ff88dd01483dc9fe6608afc6074`
- اندازهٔ فایل: `9,350,505` بایت.
- آزمون خودکار جای آزمون روی لانچر واقعی گوشی، محدودیت باتری سازنده، مجوز اعلان کاربر و لمس فیزیکی را نمی‌گیرد.

## نسخهٔ ۱.۲.۱ — ذخیرهٔ مطمئن و فضای کاری دسکتاپ

- خطای خاموش ذخیرهٔ توضیحات برطرف شد: نتیجهٔ ناموفق Firestore اکنون تشخیص داده می‌شود و تغییر در صف آفلاین قرار می‌گیرد.
- توضیحات حین تایپ با تأخیر کوتاه ذخیره می‌شود، هنگام رفتن برنامه به پس‌زمینه فوراً ارسال می‌شود و یک نسخهٔ موقت محلی تا پایان ذخیره نگه داشته می‌شود.
- وضعیت‌های «تغییرات ذخیره‌نشده»، «در حال ذخیره»، «ذخیره شد»، «منتظر همگام‌سازی» و «ذخیره ناموفق» به‌وضوح نمایش داده می‌شوند.
- پنل نیمه‌باز Android، پنل کناری دسکتاپ و صفحهٔ کامل دکمهٔ ذخیره دارند. از پنل می‌توان تسک را در صفحهٔ کامل باز کرد.
- بستن پنل یا برگشت از صفحه با تغییرات باقی‌مانده، انتخاب ذخیره و خروج، خروج بدون ذخیره یا ادامهٔ ویرایش را نشان می‌دهد. بستن تب مرورگر نیز هشدار استاندارد مرورگر را فعال می‌کند.
- پوستهٔ دسکتاپ دیگر داخل یک ستون باریک سراسری محدود نیست. صفحهٔ Tasks از شبکهٔ پهن و متعادل فهرست/ویرایشگر استفاده می‌کند و پنل جزئیات تمام ارتفاع مفید را در اختیار دارد.
- نسخهٔ Android برابر `1.2.1` و کد نسخه برابر `7` است.
- خروجی نهایی `artifacts/ARSHNAZ-1.2.1-debug.apk` است؛ SHA-256 آن `d05fbbc66760c070fc4cbd02e4dccf232c8ace82cc92b6a62a99b2da74ca4f22` و SHA-1 گواهی آن `db665e841ad598744063e2743cc532fb7d51787d` است.

## نسخهٔ 1.2.2 — منوی پوشهٔ موبایل و ذخیرهٔ کامل تسک

- عرض منوی کناری موبایل از ۱۸ به حداکثر ۲۴ رم افزایش یافت و در گوشی‌های باریک فقط ۲۰ پیکسل از صفحهٔ پشت آن نمایان می‌ماند.
- هر پوشه اکنون به‌صورت یک ردیف بزرگ و لمسی با نام، توضیح دوخطی، رنگ و سلسله‌مراتب زیرفولدر نمایش داده می‌شود. اگر توضیحی ثبت نشده باشد، راهنمای کوتاه مرتبط نمایش داده می‌شود.
- سیستم پیش‌نویس و تشخیص تغییرات از «فقط توضیحات» به تمام فیلدهای قابل ویرایش تسک گسترش یافت: عنوان، توضیحات، اولویت، پوشه، تاریخ، یادآوری، تکرار، والد، پین، تایم‌بلاک، مدت و سایر تنظیمات.
- دکمهٔ ذخیره اکنون همهٔ تغییرات معوق را با یک درخواست ثبت می‌کند؛ ذخیرهٔ خودکار، صف آفلاین، بازیابی محلی، هشدار بستن صفحه و پنجرهٔ «ذخیره و خروج» نیز تمام همین فیلدها را پوشش می‌دهند.
- نسخهٔ Android برابر `1.2.2` و کد نسخه برابر `8` است.
- خروجی قابل نصب `artifacts/ARSHNAZ-1.2.2-debug.apk` است؛ SHA-256 آن `f8b4a2cd2d50c152b97e5c79c54ba43dfe71e0d912ba2d5286d51301c6569fe4` و SHA-1 گواهی آن `db665e841ad598744063e2743cc532fb7d51787d` است.

## نسخهٔ 1.2.3 — بازبینی نهایی وب و اندروید

- مسیر بازگشت بعد از ورود در برابر URLهای خارجی، مسیرهای `//` و مسیرهای دارای backslash ایمن شد تا هدایت ناخواسته به خارج از برنامه ممکن نباشد.
- دکمهٔ ورود مهمان که در Firebase فعال نبود، از نسخهٔ عادی حذف شد و فقط در Development و با `VITE_ENABLE_GUEST_LOGIN=true` قابل نمایش است.
- عبارت‌های آزمایشی `Sync Test` از عنوان مرورگر، صفحهٔ ورود و سایدبار حذف شدند.
- صفحهٔ ورود در نمایشگرهای کم‌ارتفاع دیگر از بالا بریده نمی‌شود و با اسکرول عمودی سالم نمایش داده می‌شود.
- صفحهٔ ورود در نمای موبایل ۳۹۰×۸۴۴ و دسکتاپ از نظر بریدگی، دسترسی به ورود گوگل و خطاهای Console بررسی شد.
- نسخهٔ Android برابر `1.2.3` و کد نسخه برابر `9` است.
- خروجی قابل نصب `artifacts/ARSHNAZ-1.2.3-debug.apk` است؛ SHA-256 آن `98de39de8099ec68f8188927eb95e033da690267529b1564554fdc0fe796a25e` و SHA-1 گواهی آن `db665e841ad598744063e2743cc532fb7d51787d` است.
- در کنترل نهایی ۷۱ تست Web/React و ۲۸ تست Android موفق شدند؛ production build، Capacitor sync، Android Lint، assemble و بررسی امضای APK نیز موفق بودند.

## نسخهٔ 1.2.4 — ورودی صوتی بومی Android

- علت اصلی کارنکردن دکمه‌های میکروفون در Android رفع شد: برنامه دیگر داخل WebView به Web Speech API وابسته نیست و از `SpeechRecognizer` بومی Android استفاده می‌کند.
- مجوز `RECORD_AUDIO` به Manifest و درخواست مجوز در زمان اولین استفاده اضافه شد؛ ردکردن مجوز به‌صورت پیام قابل فهم به کاربر نمایش داده می‌شود.
- فارسی `fa-IR` و انگلیسی `en-US` از همان انتخاب‌گر زبان موجود پشتیبانی می‌شوند. موتور سیستم در حالت ترکیبی از سرویس آنلاین یا بستهٔ گفتار آفلاین نصب‌شده روی گوشی استفاده می‌کند.
- خطاهای نبود صدا، نبود سرویس تشخیص، نیاز به اینترنت/بستهٔ آفلاین و مشغول‌بودن موتور دیگر فقط در Console پنهان نیستند و در رابط برنامه نمایش داده می‌شوند.
- نسخهٔ وب همچنان از Web Speech API مرورگر استفاده می‌کند و رفتار آن با نسخهٔ Android مخلوط نشده است.
- مدل Whisper داخل APK قرار نگرفت؛ مدل آفلاین مستقل باید در مرحلهٔ بعد به‌شکل دانلود اختیاری و متناسب با توان دستگاه اضافه شود تا APK پایه صدها مگابایت بزرگ نشود.
- نسخهٔ Android برابر `1.2.4` و کد نسخه برابر `10` است.

## نسخهٔ 1.3.0 — صوت آفلاین و دستیار خصوصی

- در بخش تنظیمات → AI یک پنل «صدا و دستیار آفلاین» اضافه شد. کاربر می‌تواند موتور سیستم Android، Whisper سبک یا Whisper متعادل را انتخاب کند.
- مدل‌های Whisper همراه APK ارسال نمی‌شوند. با انتخاب آگاهانهٔ کاربر، مدل چندزبانه از منبع مدل دریافت و در Cache خود WebView ذخیره می‌شود؛ پس از دریافت نخست، گفتار فارسی و انگلیسی بدون اینترنت پردازش می‌شود.
- پیش از دانلود، اندازهٔ تقریبی و RAM پیشنهادی نمایش داده می‌شود. در حالت گزارش‌شدن RAM ناکافی یا نبود اینترنت، دانلود آغاز نمی‌شود.
- موتور اجرای محلی حدود ۲۳ مگابایت به بستهٔ Android اضافه می‌کند؛ این هزینه برای اجرای واقعی ONNX/WASM است. مدل‌های حدود ۷۵ و ۱۴۲ مگابایتی همچنان اختیاری‌اند و داخل APK وجود ندارند.
- با فعال‌کردن «دستیار آفلاین»، مسیرهای تبدیل متن به تسک، پیشنهاد اولویت/تاریخ، زیرتسک و خلاصهٔ کوتاه در نبود اینترنت یا نبود API key از منطق خصوصی و قطعی روی دستگاه استفاده می‌کنند. خروجی فقط پیشنهاد است و هیچ تغییر یا حذفی را بدون تأیید کاربر انجام نمی‌دهد.
- نسخهٔ Android برابر `1.3.0` و کد نسخه برابر `11` است.
